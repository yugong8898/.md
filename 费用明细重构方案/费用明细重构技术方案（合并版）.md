# 费用明细组件重构技术方案（合并版）

> 整理人：王新骏
> 范围：CostMessageDetails.vue 组件内部 + edit.vue / read.vue 中直接操作费用明细数据的逻辑
> 目标：将费用明细从 1257 行"上帝组件"改造为职责清晰、计算可控、bug 清零的状态
> 原则：每个任务只做一件事，改完当天能测完，出问题可秒回滚，所有改动不改变外部行为

---

## 一、范围界定

### 核心文件

| 文件路径 | 行数 | 职责 |
|---------|------|------|
| `logisticsweb/src/views/userBase/waybillSettlement/components/CostMessageDetails.vue` | 1257 | 费用明细核心组件：表格 CRUD + 汇总计算 + 抹零 + 油气优惠 + 运费差价 + 附件管理 |
| `logisticsweb/src/views/userBase/waybillSettlement/components/add.conf.js` | 17 | 配置 mixin（弹窗按钮） |
| `logisticsweb/src/views/userBase/waybillSettlement/edit.vue` | 1689 | 编辑页：费用明细模板(145-162行)、updBusCostInput(1084-1106行)、changeWaybillStatus(1220-1278行)、newServiceCharge(1333-1335行) |
| `logisticsweb/src/views/userBase/waybillSettlement/read.vue` | 947 | 查看页：费用明细模板(121-128行) |

### 不在本次范围内

- read/edit 页面级 mixin 提取（运单信息、结算信息、操作记录等）
- 风控预警逻辑
- 附件预览组件本身
- 亏吨弹窗
- 装卸重量编辑弹窗

---

## 二、现状诊断

### 2.1 组件职责过载

CostMessageDetails.vue（1257 行）同时承担以下 7 项职责：

| # | 职责 | 涉及方法 | 行数占比 |
|---|------|---------|---------|
| 1 | 费用表格 CRUD | addTableRow / delTableRow / costTypeChange / handleSelect | ~25% |
| 2 | 费用汇总计算 | handleGetSummaries / summariesFn / calcSubtotal(待提取) | ~15% |
| 3 | 运费差价计算 | countMoney / count / getBit / formatService | ~10% |
| 4 | 抹零计算 | dealZeroModeFn / processNumber | ~10% |
| 5 | 油气优惠 | updateOilGasDiscountIfNeeded / calculateOilRebate | ~10% |
| 6 | 附件管理 | viewFiles / fileRemove / uploadSuccess / handleLookImg | ~15% |
| 7 | 数据校验与整理 | commit / formItemRules / mapObj | ~15% |

### 2.2 已知 Bug 清单（共 9 个）

| # | Bug | 影响 | 根因 | 来源 |
|---|-----|------|------|------|
| B1 | countMoney 硬编码 `newList[0]` | B1 企业（companyType=1）时金额写错行（写到货物保障行而非运费差价行） | `this.$set(this.newList[0], ...)` 不判断目标行身份 | 方案A |
| B2 | `debounce(this.dealZeroModeFn(), 500)` 写法有误 | dealZeroModeFn 每次立即执行，debounce 完全无效，抹零计算未防抖 | `debounce()` 应包裹函数引用，而非调用结果 | 方案A+B |
| B3 | `serviceRateMoney` 未在 data 中声明 | 非响应式隐式属性，模板不追踪变化 | `this.serviceRateMoney = ...` 直接赋值，未声明 | 方案A+B |
| B4 | delTableRow 删除抹零行后 dealZeroModeFn 用旧数据算 | 删除抹零行走异步接口，接口成功后才 splice，此时 dealZeroModeFn 已经用旧数据算完了 | dealZeroModeFn 在 delTableRow 同步阶段调用，而非接口成功回调中 | 方案A |
| B5 | commit() 重复编码校验 forEach bug | `return false` 对 forEach 无效，校验不通过时仍继续执行 | forEach 内嵌 `for...of map` 用 `return false` 试图中断 | 方案A |
| B6 | 抹零弹窗取消后 costNewList 未清理（edit.vue 侧） | 下次保存抹零行重复计入 | edit.vue `dialogConfirmZeroInfo` 的 catch 空操作 | 方案A |
| B7 | 保存前运费差价修正用 `newList[0]`（edit.vue 侧） | 与 B1 同类问题，B1 企业时取错行 | edit.vue `changeWaybillStatus` 中 `this.$refs.CostMessageDetails.newList[0]` | 方案A |
| B8 | `updBusCostComputed` 是无 setter 的 computed，却用于 `v-model` | Vue2 控制台报错，input 赋值时静默失败 | computed 无 setter，v-model 尝试写入触发报错 | 方案B |
| B9 | `this.$forceUpdate()` 作为响应式补救 | 掩盖 newList 响应式缺陷，全组件重渲染性能浪费 | $set 更 newList 后 el-table summary-method 不自动重算 | 方案B |

### 2.3 结构性问题（共 8 个）

| # | 问题 | 位置 | 影响 | 来源 |
|---|------|------|------|------|
| S1 | newList 双重初始化 | mounted + detailObj watch | mounted 结果立刻被 watch 覆盖；两处全流程处理不一致（mounted 不强制 premiumAmount=0） | 方案A |
| S2 | 小计在 summary-method 里赋值 | handleGetSummaries | el-table 渲染驱动调用，触发时机不可控；UI 渲染与业务计算混合 | 方案A |
| S3 | 油气优惠每次 blur 调接口 | updateOilGasDiscountIfNeeded | 连续修改多行时大量无效请求；无竞态保护，旧请求可能覆盖新请求 | 方案A |
| S4 | searchOptions 全组件共享 | data.searchOptions | 多行同时 focus 费用项目下拉时互相覆盖选项 | 方案A |
| S5 | 父子通过 $refs 深度耦合 | edit.vue → CostMessageDetails | `ref.newList[0]` / `ref.commit()` / `ref.countMoney()` / `ref.dealZeroModeFn()` / `ref.subtotal` 5 处直接操作子组件内部 | 方案A+B |
| S6 | count/formatService/getBit 重复 | CostMessageDetails + endOfCalculation.js | 同一计算逻辑两处维护，改一处忘改另一处 | 方案A |
| S7 | 油气优惠判断逻辑重复 3 次 | mounted(467-486) + detailObj watcher 首次构建(395-433) + detailObj watcher 已有数据分支(344-383) | 改规则需改 3 处，漏改概率高 | 方案B |
| S8 | 第二张汇总表占约 250 行，与主表格 CRUD 无关 | summaryTable + summariesFn + newList + tableNewList | 职责混杂，改汇总行要翻过大量无关代码 | 方案B |

### 2.4 废弃代码

| 字段/方法 | 说明 |
|-----------|------|
| `total` | data 中声明，从未被赋值使用 |
| `arr` | data 中声明，从未使用 |
| `nowIcon` | data 中声明，从未使用 |
| `gxType` | computed 中声明，模板和 methods 均未使用 |
| `dialogImgVisible` | 已被 `el-image-x` 替代，但字段仍被 `handleLookImg` 写入 |
| `handleDialogClose` | 旧图片弹窗回调，已无调用方 |
| `display:black` | 原金额列 style 笔误，应为 `display:block` |

---

## 三、横向影响检查

| 检查项 | 是否涉及 | 文件/说明 |
|--------|---------|----------|
| 调整页（edit.vue） | 是 | 费用明细模板、保存/提交流程中的费用校验与抹零 |
| 查看页（read.vue） | 是 | 费用明细模板（只读模式） |
| 列表页 | 否 | 不涉及费用明细组件 |
| 对账页 | 否 | 不涉及费用明细组件 |
| 付款页 | 否 | 不涉及费用明细组件 |
| 移动端对应页面 | 否 | 货主移动端无费用明细编辑功能 |
| 公共组件 | 是 | CostMessageDetails.vue 本身是公共组件，被 edit/read 共用 |
| 接口封装 | 否 | 本次不改动接口调用逻辑 |

---

## 四、重构策略：四阶段渐进式

```
阶段一 P0：安全修复（9 个任务，互相独立，可并行提测）
  ↓ 修 9 个已知 bug，每个 ≤15 行
  ↓ 9 个 PR，可并行提测

阶段二 P1：结构收拢（7 个任务，依赖 P0 完成）
  ↓ 计算分离 + 初始化统一 + 废弃清理 + 接口防抖 + 逻辑收拢
  ↓ 7 个 PR，建议顺序执行

阶段三 P2：组件拆分与解耦（4 个任务，依赖 P1 完成）
  ↓ 纯函数提取 + 汇总表拆分 + 附件拆分 + 父子通信改 emit
  ↓ 4 个 PR，需完整回归

阶段四 P3：远期优化（1 个任务，可选，高风险）
  ↓ 抹零计算单一来源
  ↓ 需充分评估后执行
```

---

## 五、P0 批次：安全修复（9 个任务）

> 目标：修复 9 个确认 bug，不改变任何业务行为，每个任务独立提交独立验证。
> 互相独立，可并行提测（P0-7 依赖 P0-1）。

### P0-1：countMoney 硬编码下标修复

**文件**：CostMessageDetails.vue | **改动量**：~5 行 | **修复 B1**

**现状**：L555~557，`countMoney` 通过 `this.newList[0]` 硬编码取第一行写入运费差价。当 `companyType=1`（B1 企业）时 `newList[0]` 是货物保障服务行，金额写错位置。

**改法**：
```js
// 改前（L555~557）
this.$set(this.newList[0], 'confirmMoney', this.serviceRateMoney)
this.$set(this.newList[0], 'confirmRemark', rate)

// 改后
const freightDiffRow = this.newList.find(i => i.costName === '运费差价')
if (freightDiffRow) {
  this.$set(freightDiffRow, 'confirmMoney', this.serviceRateMoney)
  this.$set(freightDiffRow, 'confirmRemark', rate)
}
```

**为什么安全**：`find` 找到的就是原来 `[0]` 指向的对象（普通运单），行为完全一致；B1 时 `find` 返回 undefined，`if` 保护不执行。

**验证**：
- □ 普通运单：修改确认金额 → 运费差价行联动更新
- □ B1 企业运单（companyType=1）：无运费差价行，控制台无报错
- □ 保存时运费差价值正确写入后端

---

### P0-2：debounce 写法修复

**文件**：CostMessageDetails.vue + edit.vue | **改动量**：~10 行 | **修复 B2**

**现状**：
- CostMessageDetails.vue L588：`debounce(this.dealZeroModeFn(), 500)` 传入了函数执行结果（undefined），debounce 完全无效
- edit.vue L1103：`debounce(this.$refs.CostMessageDetails.dealZeroModeFn(), 500)` 同类问题

**改法**：
```js
// CostMessageDetails.vue：在 created 中预创建防抖函数
created() {
  this._debouncedDealZero = debounce(() => {
    this.dealZeroModeFn()
  }, 500)
}

// updBusCostInput 中
// [重构前] debounce(this.dealZeroModeFn(), 500)
this._debouncedDealZero()

// edit.vue L1103：改为直接调用（保持原有效行为：立即执行）
// [重构前] debounce(this.$refs.CostMessageDetails.dealZeroModeFn(), 500)
this.$refs.CostMessageDetails.dealZeroModeFn()
```

**为什么安全**：防抖只是延后执行，最终一定会执行一次；edit.vue 侧改为立即执行保持原有行为不变。

**验证**：
- □ 修改 1 行确认金额：500ms 后抹零行更新
- □ 连续修改 3 行：只有最后一次触发抹零计算
- □ 修改后立即点保存：debounce 未执行时保存行为正确（保存时 changeWaybillStatus 会独立计算）

---

### P0-3：serviceRateMoney 响应式声明

**文件**：CostMessageDetails.vue | **改动量**：1 行 | **修复 B3**

**现状**：L553，`this.serviceRateMoney = ...` 直接赋值，但 `serviceRateMoney` 未在 `data()` 中声明，是隐式非响应式属性。

**改法**：
```js
// data() return 中新增
serviceRateMoney: 0  // ← 新增
```

**为什么安全**：声明初始值不影响已有赋值逻辑；如果模板未使用此字段（当前未使用），声明只是让 Vue 追踪到它，无副作用。

**验证**：
- □ 运费差价计算正常
- □ Vue DevTools 中 serviceRateMoney 可见

---

### P0-4：delTableRow 异步时序修复

**文件**：CostMessageDetails.vue | **改动量**：~5 行 | **修复 B4**

**现状**：`delTableRow` 删除抹零行时走异步接口 `getZeroDelStatus`，接口成功后才 splice 行。但 `delTableRow` 在调用 `getZeroDelStatus` 后**同步**执行了 `dealZeroModeFn()`，此时行还未真正删除，抹零计算用的是旧数据。

**改法**：
```js
// [重构前] delTableRow 末尾同步调用
delTableRow(index) {
  // ...
  if ([101, 102].includes(row.orderNumber)) {
    this.getZeroDelStatus(index)  // 异步删除
  } else {
    this.params.model.splice(index, 1)
  }
  this.dealZeroModeFn()  // ← 异步删除还没完成，用的是旧数据
}

// 改后：抹零重算移到各自的删除分支中
delTableRow(index) {
  // ...
  if ([101, 102].includes(row.orderNumber)) {
    this.getZeroDelStatus(index)
  } else {
    this.params.model.splice(index, 1)
    this.dealZeroModeFn()  // 同步删除，立即重算
  }
  // 不再在这里统一调 dealZeroModeFn
}

getZeroDelStatus(index) {
  // ... 调接口 ...
  if (res.success) {
    this.params.model.splice(index, 1)
    this.dealZeroModeFn()  // ← 移到这里，行已删除
    this.$emit('updateZeroType')
  }
}
```

**验证**：
- □ 删除抹零行后：抹零行消失，合计金额正确更新
- □ 删除普通费用行后：抹零计算正确
- □ 删除抹零行接口报错：行不被删除，抹零金额不变

---

### P0-5：commit() 重复编码校验修复

**文件**：CostMessageDetails.vue | **改动量**：~10 行 | **修复 B5**

**现状**：`commit()` 方法内用 `forEach` 遍历费用行，嵌套 `for...of map` 校验重复编码，发现重复时 `return false`。但 `return false` 对 `forEach` 无效，校验不通过时仍继续执行后续逻辑。

**改法**：
```js
// [重构前] forEach + return false（无效）
params.model.forEach(item => {
  const map = new Map()
  for (let [key, val] of map) {
    if (val > 1) {
      this.$message.error('...')
      return false  // ← 对 forEach 无效！
    }
  }
})

// 改后：用 for...of 可中断循环
for (const item of params.model) {
  const map = new Map()
  for (const row of params.model) {
    if (row.costType === item.costType) {
      const key = row.costCode || row.costName
      map.set(key, (map.get(key) || 0) + 1)
    }
  }
  for (let [key, val] of map) {
    if (val > 1) {
      this.$message.error(`费用类型下存在重复费用编码：${key}`)
      return false  // ← 对外层方法 return，可正常中断
    }
  }
}
```

**验证**：
- □ 正常费用明细保存：校验通过，返回整理后的数组
- □ 存在重复编码：报错并返回失败标记
- □ 多组重复编码：只报第一个重复项（符合预期）

---

### P0-6：抹零取消后 costNewList 清理

**文件**：edit.vue | **改动量**：~5 行 | **修复 B6**

**现状**：`dialogConfirmZeroInfo` 弹窗的 `.catch()` 空操作，用户取消抹零后 `costNewList` 里已 push 的抹零行（orderNumber 101/102）未清理，下次保存重复计入。

**改法**：
```js
// [重构前]
.catch(() => { })

// 改后
.catch(() => {
  this.costNewList = this.costNewList.filter(i => ![101, 102].includes(i.orderNumber))
})
```

**验证**：
- □ 抹零弹窗点"确定"：正常保存
- □ 抹零弹窗点"取消"：不保存，再次保存时抹零行不重复
- □ 连续取消 3 次再确定：只有 1 行抹零行

---

### P0-7：保存前运费差价修正去硬编码

**文件**：edit.vue | **改动量**：~3 行 | **修复 B7** | **前置依赖**：P0-1

**现状**：`changeWaybillStatus` 中通过 `this.$refs.CostMessageDetails.newList[0]?.confirmRemark` 读取差价率，与 B1 同类问题。

**改法**：
```js
// [重构前]
const currentTaxRate = this.$refs.CostMessageDetails.newList[0]?.confirmRemark || this.detail.taxRate

// 改后
const freightDiffRow = this.$refs.CostMessageDetails.newList.find(i => i.costName === '运费差价')
const currentTaxRate = freightDiffRow?.confirmRemark || this.detail.taxRate
```

**验证**：
- □ 有抹零的运单保存：运费差价正确
- □ 无抹零的运单保存：运费差价不变
- □ B1 企业运单保存：不报错

---

### P0-8：updBusCostComputed v-model 修复

**文件**：CostMessageDetails.vue | **改动量**：~3 行 | **修复 B8**

**现状**：L48-56 模板中 `v-model.trim="updBusCostComputed"` 绑定了无 setter 的 computed，Vue2 控制台报错。F10102（按比例固定扣款）的 input 始终为 `:disabled` 状态。

**改法**：
```html
<!-- [重构前] -->
<el-input v-model.trim="updBusCostComputed" ... />

<!-- 改后：单向绑定，展示值仍由 computed 驱动 -->
<el-input :value="updBusCostComputed" ... />
```

**为什么安全**：F10102 的 input 始终 disabled，用户无法输入，v-model 的 setter 从未被真正触发；改为 `:value` 后展示效果完全一致。

**验证**：
- □ 控制台不再有 computed setter 警告
- □ F10102 费项行的确认金额正确展示（= 运费 x 系数）
- □ 修改运费确认金额，F10102 行联动更新

---

### P0-9：移除 $forceUpdate，改用 doLayout

**文件**：CostMessageDetails.vue | **改动量**：~3 行 | **修复 B9**

**现状**：L742，`updateOilGasDiscountIfNeeded` 方法中 `this.$forceUpdate()` 作为响应式补救。$set 更新 newList[oilGasIndex].confirmMoney 后，el-table 的 summary-method 不会自动重算。

**改法**：
```js
// [重构前]
this.$forceUpdate()

// 改后：精准触发 el-table 重算
this.$nextTick(() => {
  this.$refs.summaryTable && this.$refs.summaryTable.doLayout()
})
```

**为什么安全**：代码中已有用 `doLayout()` 触发重算的先例（detailObj watcher L388-390）；`doLayout()` 会触发 el-table 重新调用 `summariesFn`，效果与 `$forceUpdate` 一致但更精准。

**验证**：
- □ 编辑页修改司机运费，油气优惠行金额实时更新（不依赖 $forceUpdate）
- □ 有油气费 / 无油气费 / 全流程(fullProcessFlag=10) 三种场景分别验证

---

### P0 交付顺序

```
第 1 批（互相独立，可并行提测）：
  P0-1  countMoney 下标修复（CostMessageDetails）
  P0-2  debounce 写法修复（CostMessageDetails + edit.vue）
  P0-3  serviceRateMoney 声明（CostMessageDetails）
  P0-4  delTableRow 时序修复（CostMessageDetails）
  P0-5  commit 校验修复（CostMessageDetails）
  P0-6  抹零取消清理（edit.vue）
  P0-8  v-model 修复（CostMessageDetails）
  P0-9  $forceUpdate 修复（CostMessageDetails）

依赖链：
  P0-7  保存前修正去硬编码（edit.vue，依赖 P0-1）

观察期：合入后观察 1~2 天
```

---

## 六、P1 批次：结构收拢（7 个任务）

> 目标：消除重复逻辑，不拆组件，不改变对外接口。
> 依赖 P0 完成，建议顺序执行。

### P1-1：油气优惠判断逻辑收拢为单一方法

**文件**：CostMessageDetails.vue | **改动量**：~40 行 | **解决 S7**

**做什么**：
1. 新增方法 `getOilGasConfig(detailObj)`，返回 `{ shouldShow, oilAmount, oilDiscountAmount }`
   - 收录 `hasValidOilData` 判断逻辑
   - 收录 `fullProcessFlag === 10` 覆盖逻辑（强制显示，金额置 0）
2. 新增方法 `buildOilGasItem(detailObj)`，返回 `{ costName: '油气优惠', confirmMoney, confirmRemark }` 或 `null`
   - `confirmMoney` 统一为 `detailObj.fullProcessFlag === 10 ? 0 : (oilDiscountAmount || 0)`
3. 替换三处重复：mounted(467-486) + detailObj watcher 已有数据分支(344-383) + detailObj watcher 首次构建分支(395-433)

**行为差异说明**：mounted 原来不处理 `fullProcessFlag === 10`，统一后 mounted 也会处理。但 watcher 随后触发时会检测到已存在油气优惠行而不重复添加，因此最终结果一致。

**验证**：
- □ 有油气费运单：查看/编辑页油气优惠行正常展示，金额正确
- □ 无油气费运单：不展示油气优惠行
- □ 全流程运单(fullProcessFlag=10)：油气优惠展示为 0
- □ 编辑页修改运费后：油气优惠金额通过接口实时更新

**收益**：油气优惠判断从 3 处重复变为 1 处，规则变更只改 1 处

---

### P1-2：油气接口 debounce + 竞态保护

**文件**：CostMessageDetails.vue | **改动量**：~15 行 | **解决 S3**

**做什么**：`updateOilGasDiscountIfNeeded` 加 debounce 500ms + 竞态保护

```js
// data 里加 _oilGasRequestId: 0
updateOilGasDiscountIfNeeded: debounce(async function() {
  if (this.detailObj.fullProcessFlag === 10) return
  const oilAmount = this.detailObj?.oilAmount || this.detailObj?.bmsWaybillPoolExt?.oilAmount
  if (!oilAmount || Number(oilAmount) <= 0) return

  const requestId = ++this._oilGasRequestId
  // ... 原有接口调用逻辑不变 ...
  if (requestId !== this._oilGasRequestId) return  // 旧请求丢弃
  // ... 原有更新逻辑不变 ...
}, 500)
```

**验证**：
- □ 修改 1 行 blur：油气优惠正常更新
- □ 连续修改 3 行 blur：只有最后一次触发接口（Network tab 验证）
- □ 全流程运单：油气优惠始终为 0，无接口调用

---

### P1-3：小计计算与 handleGetSummaries 分离

**文件**：CostMessageDetails.vue | **改动量**：~20 行 | **解决 S2**

**做什么**：新增独立计算方法 `calcSubtotal()`，`handleGetSummaries` 改为纯展示读取已计算的 `this.subtotal`

```js
calcSubtotal() {
  const sum = this.params.model.reduce((total, item) => {
    if ([101, 102].includes(item.orderNumber)) return total
    if (['200', '600'].includes(`${item.costType}`)) {
      return total.plus(+(item.updBusCost || 0))
    } else {
      return total.minus(+(item.updBusCost || 0))
    }
  }, new BigNumber(0))
  this.subtotal = Number(sum)
}

// handleGetSummaries 改为纯展示，读取已计算的 this.subtotal
// updBusCostInput 里显式调用 this.calcSubtotal()
```

**安全策略**：第一版两边都保留赋值（双写），验证一周后再删 handleGetSummaries 里的赋值。

**验证**：
- □ 进入编辑页：小计展示正确
- □ 修改确认金额：小计实时更新
- □ 新增/删除费用行：小计正确
- □ 运费差价联动：subtotal 变化 → countMoney 正确触发

---

### P1-4：newList 构建统一为 buildNewList

**文件**：CostMessageDetails.vue | **改动量**：删除 ~50 行 + 新增 ~25 行 | **解决 S1** | **前置依赖**：P1-1

**做什么**：
1. 新增方法 `buildNewList(detailObj)`，统一构建 newList（运费差价 + 货物保障服务 + 油气优惠）
   - 处理 `companyType === 1` 不显示运费差价
   - 处理 `fullProcessFlag === 10` 时货物保障服务金额为 0
   - 调用 `buildOilGasItem` 添加油气优惠行（依赖 P1-1）
2. mounted 中的 newList 初始化改为调用 `this.buildNewList(this.detailObj)`
3. detailObj watcher 首次构建分支改为调用 `this.buildNewList(this.detailObj)`
4. detailObj watcher 已有数据分支：只更新油气行，调用 `getOilGasConfig` 判断

```js
mounted() {
  window.addEventListener('resize', this.handleResize)
  const { type } = this.$route.query
  this.type = type
  this.busBillId = this.params.busBillId
}

watch: {
  detailObj(val) {
    this.$nextTick(() => {
      if (this.newList.length === 0 || this.type !== 'edit') {
        this._buildNewList(val)       // 完整重建
      } else {
        this._updateOilGasOnly(val)   // 编辑模式：只更新油气行
      }
    })
  }
}
```

**验证**：
- □ 查看/编辑页：运费差价行、货物保障服务行、油气优惠行正常展示
- □ companyType=1：不显示运费差价行
- □ companyType=2：显示运费差价行
- □ premiumBuyerType='10'：tableNewList 过滤掉货物保障服务行
- □ 全流程运单：货物保障=0，油气优惠=0
- □ 保存后刷新：newList 正确重建

**收益**：newList 构建逻辑从 2 处重复变为 1 处，消除首次加载与 watcher 不一致风险

---

### P1-5：废弃字段清理

**文件**：CostMessageDetails.vue | **改动量**：删除 ~15 行 | **无风险**

| 字段/方法 | 行号 | 操作 |
|-----------|------|------|
| `total` | data L276 | 删除声明 |
| `arr` | data L272 | 删除声明 |
| `nowIcon` | data L256 | 删除声明 |
| `gxType` | computed L306 | 删除整个 computed |
| `dialogImgVisible` | data L263 | 删除声明 + L758 赋值 |
| `handleDialogClose` | methods L763 | 删除方法 |
| `display:black` | 模板 L35 | 改为 `display:block` |

**验证**：
- □ 组件正常渲染，无报错
- □ 图片预览功能正常（el-image-x 替代）
- □ 原金额列展示正常

---

### P1-6：searchOptions 行级隔离

**文件**：CostMessageDetails.vue | **改动量**：~20 行 | **解决 S4**

**做什么**：将 `searchOptions` 从全局 data 字段改为行级数据

```js
// [重构前] data 中全局共享
data() { return { searchOptions: [] } }

// 改后：在 params.model 每行增加 _searchOptions 字段
getSearchList(row) {
  // ... 拉取选项 ...
  this.$set(row, '_searchOptions', list)
}

// 模板改为读取行级选项
<el-option v-for="item in (scope.row._searchOptions || [])" ... />
```

**验证**：
- □ 单行 focus 费用项目下拉：选项正确
- □ 快速切换两行 focus：各自选项不互相覆盖
- □ 保存时 `_searchOptions` 不被提交（commit 中过滤）

---

### P1-7：commit() 返回值规范化

**文件**：CostMessageDetails.vue + edit.vue | **改动量**：~15 行

**做什么**：`commit()` 返回值从混合类型（数组/字符串）改为统一结构

```js
// [重构前] 返回整理好的数组（成功）或 '1'/'2'（失败）

// 改后
commit() {
  if (校验失败) return { valid: false, error: '重复编码' }
  if (表单校验未通过) return { valid: false, error: 'form' }
  return { valid: true, data: 整理后的数组 }
}

// edit.vue 调用处
const result = this.$refs.CostMessageDetails.commit()
if (!result.valid) return
const costNewList = result.data
```

**前置确认**：搜索 `commit()` 的所有调用方（目前只有 edit.vue `changeWaybillStatus`），确保同步修改。

**验证**：
- □ 正常保存：返回 `{ valid: true, data: [...] }`
- □ 重复编码：返回 `{ valid: false, error: '...' }`，流程中止
- □ 表单校验不通过：返回 `{ valid: false, error: 'form' }`

---

### P1 交付顺序

```
第 2 批（建议顺序执行）：
  P1-1  油气优惠逻辑收拢 ──→ P1-4 依赖此项
  P1-2  油气接口 debounce
  P1-3  小计与 UI 分离 ────→ P3-1 依赖此项
  P1-4  newList 构建统一（依赖 P1-1）
  P1-5  废弃字段清理
  P1-6  searchOptions 行级隔离
  P1-7  commit() 返回值规范 ──→ P2-4 依赖此项

观察期：合入后观察 2~3 天
```

---

## 七、P2 批次：组件拆分与解耦（4 个任务）

> 目标：拆分 CostMessageDetails 职责，提取纯函数，改父子通信方式。
> 依赖 P1 完成，需完整回归测试。

### P2-1：提取费用计算纯函数到 costCalculation.js

**文件**：新增 `mixins/costCalculation.js` + 修改 CostMessageDetails.vue | **解决 S6**

**做什么**：将以下方法从组件 methods 迁移到 mixin（保持方法签名不变，组件内引用不变）：
- `countMoney(cost, rate)` — 运费差价计算
- `count(Amount, Rate)` — 差价核心公式
- `getBit(value, n)` — 截取精度
- `formatService(num, n, type)` — 取整方式
- `dealZeroModeFn()` 中的核心计算部分 — 提取为 `calcZeroAmount(model, zeroByBusIdMode)` 纯函数

CostMessageDetails 保留 `count` 方法作为薄代理：
```js
import { calcTotal } from '@/mixins/costCalculation'
count(Amount, Rate) {
  return calcTotal(Amount, Rate, this.roundingMode)
}
```

`endOfCalculation.js` 暂不改，后续单独 PR 替换。

**验证**：
- □ 运费差价计算结果与改动前一致（对比 3 张不同运单）
- □ 不同 roundingMode（0/1/4）下结果一致
- □ 差价率为 0 时不报错
- □ 抹零模式=不抹零(0)/小数抹零/个位抹零，各场景金额正确

**收益**：计算逻辑可脱离 Vue 组件单测；CostMessageDetails 减少约 100 行

---

### P2-2：拆出 CostSummaryTable.vue（汇总副表）

**文件**：新增 `components/CostSummaryTable.vue` + 修改 CostMessageDetails.vue | **解决 S8**

**做什么**：
1. 将 CostMessageDetails 中第二个 `el-table`（summaryTable，第 116-149 行）迁移到新组件
2. 迁移 `summariesFn` 方法（第 862-940 行）
3. 迁移 `newList`、`tableNewList` computed、`subtotal` watch 中的 newList 重建逻辑
4. CostMessageDetails 通过 props 传递 `detailObj`、`subtotal`、`partPayData` 给 CostSummaryTable
5. CostSummaryTable 通过 `emit('newServiceCharge')` 和 `emit('updateDetail')` 向上传递

**验证**：
- □ 合计行金额正确（小计 + 运费差价 + 货物保障 - 油气优惠）
- □ companyType=1：合计行不显示，仅显示应付承运商运费
- □ companyType=2：合计行显示，显示客户应付运费
- □ fullProcessFlag=10：显示全流程运费差价行 + 差价率
- □ 分次支付运单：小计行的分次支付信息正常展示
- □ premiumBuyerType='20'：小计行展示"含司机购买货物保障服务金额"
- □ 编辑页修改运费，合计行联动更新

**收益**：CostMessageDetails 从约 1100 行（P1 后）降到约 850 行；汇总表独立维护

---

### P2-3：拆出附件管理到 useAttachment.js

**文件**：新增 `mixins/useAttachment.js` + 修改 CostMessageDetails.vue

**做什么**：将附件相关方法迁移到独立 mixin：
- `viewFiles(index, fileList)` / `uploadSuccess(...)` / `beforeUpload(val)` / `handleError()` / `fileRemove(...)` / `closeDialog()` / `handleLookImg(item)` / `handleDialogClose()`
- 相关 data：`fileList`、`activeIndex`、`upLoading`、`show.dialog`、`imgList`、`imgListTableUrl`

**验证**：
- □ 编辑页点击"上传附件"，弹窗正常打开
- □ 上传图片，成功添加到对应费用行
- □ 删除附件，从费用行移除
- □ 查看页点击"查看附件"，图片预览正常
- □ 保存时费用行的 attachmentList 包含正确附件数据

**收益**：CostMessageDetails 再减约 80 行；附件逻辑集中，便于后续维护

---

### P2-4：父子通信改 emit + getter

**文件**：CostMessageDetails.vue + edit.vue | **改动量**：~40 行 | **解决 S5** | **前置依赖**：P1-7

**现状**：edit.vue 通过 5 处 `$refs` 直接操作子组件内部：

| 调用 | 用途 | 改法 |
|------|------|------|
| `ref.commit()` | 校验并获取费用数据 | 保持（P1-7 已规范化返回值，作为对外 API 合理） |
| `ref.subtotal` | 读取小计金额 | 改为 `emit('subtotal-change', val)`，父组件监听 |
| `ref.newList[0].confirmRemark` | 读取差价率 | 改为 `emit('rate-change', rate)`，在 countMoney 中触发 |
| `ref.countMoney(amount, rate)` | 重新计算运费差价 | 保持（作为对外 API，但改名 `recalcFreightDiff`） |
| `ref.dealZeroModeFn()` | 触发抹零计算 | 保持（作为对外 API，但改名 `recalcZeroMode`） |

```js
// CostMessageDetails
watch: {
  subtotal(val) { this.$emit('subtotal-change', val) }
}
countMoney(cost, rate) {
  // ... 原有计算 ...
  this.$emit('rate-change', rate)
  this.$emit('newServiceCharge', this.serviceRateMoney)
}

// edit.vue 模板
<CostMessageDetails
  @subtotal-change="onSubtotalChange"
  @rate-change="onRateChange"
  @new-service-charge="onServiceChargeChange"
/>
```

**验证**：
- □ 修改确认金额：subtotal-change 事件正确触发
- □ 运费差价变化：rate-change 和 newServiceCharge 事件正确
- □ 保存/提交：commit() 返回值正确
- □ 抹零计算：recalcZeroMode() 正常

**收益**：父子 $refs 耦合点从 5 处降为 1 处（commit API），CostMessageDetails 可独立复用

---

### P2 交付顺序

```
第 3 批（建议顺序执行，需完整回归）：
  P2-1  计算纯函数提取
  P2-2  拆出汇总副表
  P2-3  拆出附件管理
  P2-4  父子通信改 emit（依赖 P1-7）

观察期：合入后观察 3~5 天
```

---

## 八、P3 批次：远期优化（1 个任务，可选）

> 仅在 P0-P2 全部完成且稳定后执行。风险中高，需充分评估。

### P3-1：抹零计算单一来源

**文件**：CostMessageDetails.vue + edit.vue | **前置依赖**：P1-3 + P0-2

**现状**：`dealZeroModeFn`（CostMessageDetails，实时预览）和 `changeWaybillStatus`（edit.vue，保存前）各自独立计算抹零，逻辑重复且可能不一致。

**目标**：`dealZeroModeFn` 作为抹零唯一计算来源，保存时直接使用 params.model 中已有的抹零行数据，不再在 edit.vue 中独立计算和 push。

```js
// edit.vue changeWaybillStatus 改后
const newlist = this.changeMoney(CostDetails)
this.costNewList = newlist  // 直接用，含 dealZeroModeFn 已追加的抹零行

const zeroRow = this.costNewList.find(i => [101, 102].includes(i.orderNumber))
if (zeroRow && this.zeroByBusIdMode) {
  const confirmed = await this.confirmZeroDialog(zeroRow)
  if (!confirmed) return
}
this.beforeChangeWaybill()
```

**边界场景**：
- 费用明细为空时保存
- 抹零方式为"不抹零"
- 差额为 0（不应有抹零行）
- debounce 还没执行就点了保存

**收益**：抹零计算来源从 2 处变为 1 处，消除不一致风险

---

## 九、全局依赖关系图

```
P0（安全修复）：互相独立，可并行
  P0-1  countMoney 下标修复 ──────────→ P0-7（edit.vue 侧同类修复）
  P0-2  debounce 写法修复 ───────────→ P3-1（远期，依赖防抖生效）
  P0-3  serviceRateMoney 声明
  P0-4  delTableRow 时序修复
  P0-5  commit 校验修复
  P0-6  抹零取消清理（edit.vue）
  P0-8  v-model 修复
  P0-9  $forceUpdate 修复

P1（结构收拢）：建议顺序执行
  P1-1  油气优惠逻辑收拢 ─────────→ P1-4（依赖 buildOilGasItem）
  P1-2  油气接口 debounce
  P1-3  小计与 UI 分离 ────────────→ P3-1（远期，依赖小计独立计算）
  P1-4  newList 构建统一（依赖 P1-1）
  P1-5  废弃字段清理
  P1-6  searchOptions 行级隔离
  P1-7  commit 返回值规范 ────────→ P2-4（父子通信改 emit 的前置）

P2（组件拆分与解耦）：有依赖
  P2-1  计算纯函数提取
  P2-2  拆出汇总副表
  P2-3  拆出附件管理
  P2-4  父子通信改 emit（依赖 P1-7）

P3（远期优化）：
  P3-1  抹零单一来源（依赖 P1-3 + P0-2）
```

---

## 十、收益总览

### 10.1 量化收益

| 维度 | 改前 | 全部完成后 |
|------|------|-----------|
| CostMessageDetails.vue 行数 | 1257 行 | ~600 行（-52%，含拆分） |
| 已知 bug | 9 个（3 个用户可感知 + 6 个隐性） | 0 |
| 废弃字段/方法 | 7 处 | 0 |
| 父子 $refs 耦合点 | 5 处 | 1 处（commit API） |
| 计算函数重复 | CostMessageDetails + endOfCalculation 各一份 | mixin 统一 |
| 抹零计算来源 | 2 处（dealZeroModeFn + changeWaybillStatus） | 1 处（dealZeroModeFn） |
| 小计计算触发 | el-table 渲染驱动（不可控） | 显式调用（可控） |
| 油气接口调用频率 | 每次 blur 1 次 | debounce 500ms，连续操作只调 1 次 |
| 油气优惠判断逻辑 | 3 处重复 | 1 处 |
| newList 构建逻辑 | 2 处重复 | 1 处 |
| 组件职责 | 7 项混在 1 个组件 | 3 个组件/mixin 各司其职 |

### 10.2 开发体验收益

| 场景 | 改前痛点 | 改后体验 |
|------|---------|---------|
| 追费用计算 bug | 在 handleGetSummaries/countMoney/dealZeroModeFn 之间跳转 | calcSubtotal 断点精确，链路清晰 |
| B1 企业运单 | countMoney 写错行，保存前修正取错行 | 按名查找，B1 安全跳过 |
| 新增费用类型 | 不知道该改 mounted 还是 watch | buildNewList 唯一入口 |
| 连续修改确认金额 | 油气接口大量调用，抹零每次立即执行 | debounce 保护，各只触发 1 次 |
| 删除抹零行 | 重算时序错误，用旧数据算 | 接口成功回调中重算 |
| Code Review commit() | 返回值类型不确定（数组/字符串） | `{ valid, data, error }` 结构清晰 |
| 修改油气优惠规则 | 需改 3 处，漏改概率高 | 改 1 处 |
| 维护汇总副表 | 翻过大量无关代码才能找到汇总逻辑 | 独立组件，职责清晰 |
| 控制台报错 | computed setter 警告 + $forceUpdate 性能浪费 | 控制台干净 |

---

## 十一、风险控制策略

| 策略 | 说明 |
|------|------|
| 单文件单 PR | P0 阶段每个 PR 只改 1 个文件，revert 成本为 1 个 commit |
| 行为不变优先 | 先做"不改变任何计算结果"的重构，再做 bug 修复 |
| 双写过渡 | 小计分离保留旧代码注释，双写验证一周后再删 |
| 测试前置 | 每个 PR 附带手动验证 checklist，测试通过才合 |
| 渐进替换 | 父子通信改 emit 时不一次性替换所有调用点，逐个替换并验证 |
| 灰度观察 | 每批合入后观察 1~5 天（随阶段递增），出问题立即 revert |

---

## 十二、测试回归总表

| 场景 | 涉及任务 | 验证点 |
|------|---------|--------|
| 普通运单修改确认金额 | P0-1/2/9, P1-2/3 | 小计正确、运费差价正确、油气优惠更新、抹零行更新 |
| B1 企业运单 | P0-1/7 | 无运费差价行时不报错，货物保障行不被意外写入 |
| 全流程运单 | P0-9, P1-1/4 | 货物保障=0，油气优惠=0，差价从接口获取 |
| 连续修改 5 行确认金额 | P0-2, P1-2 | 抹零只算 1 次（debounce），油气接口只调 1 次 |
| 删除抹零行 | P0-4 | 抹零行消失，合计金额正确更新 |
| 删除普通费用行 | P0-4 | 抹零计算正确 |
| 保存（310） | P0-5/6/7, P1-7 | commit 返回正确，抹零行不重复，运费差价正确 |
| 提交（320） | P0-5/6/7, P1-7 | 同上 + 风控前费用数据正确 |
| 抹零弹窗取消 | P0-6 | costNewList 不残留抹零行 |
| 费用项目下拉 | P1-6 | 多行同时 focus 不互相覆盖 |
| 图片预览 | P1-5 | el-image-x 正常工作 |
| 原金额列展示 | P1-5 | display:block 正确 |
| F10102 按比例扣款 | P0-8 | 控制台无 setter 警告，金额正确展示 |
| 油气优惠金额更新 | P0-9, P1-1/2 | 修改运费后油气优惠实时更新 |
| 运费差价计算 | P2-1 | 不同 roundingMode 下结果一致 |
| 汇总行联动 | P2-2 | 合计行金额随费用变化实时更新 |
| 附件上传/删除/预览 | P2-3 | 上传/删除/预览功能正常 |
| 父子通信 | P2-4 | emit 事件正确触发，无 $refs 残留 |
| 抹零单一来源 | P3-1 | 保存时抹零数据与页面展示一致 |

---

## 十三、数据结构设计

### 本次无新增/修改字段

所有重构任务均不改变接口入参和出参，不改变 data 结构（仅在 data() 中补充声明已有的隐式属性 `serviceRateMoney`，新增内部防抖标识 `_oilGasRequestId`、`_debouncedDealZero`）。

### 接口入参/出参变化

无。所有接口调用逻辑保持不变。

---

## 十四、执行约束

1. **行为不变原则**：每个任务完成后，费用明细的展示和交互必须与重构前完全一致
2. **一次一个任务**：每个任务改完输出自检，等确认后再进入下一个
3. **旧代码注释保留**：替换旧写法时用 `// [重构前] xxx` 注释，不直接删除
4. **不引入新依赖**：不新增 npm 包，不引入新组件库（新增的 mixin/组件文件均为项目内部文件）
5. **收益必须具体**：不写"可维护性提升"，写"从改 3 处变改 1 处"、"从 1257 行降到 600 行"

---

## 十五、验收标准

| 批次 | 验收方式 | 通过标准 |
|------|---------|---------|
| P0 | 逐任务验证 + 控制台无报错 | 9 个 bug 修复，控制台无 Vue warning |
| P1 | 逐任务验证 + 对比重构前后行为 | 油气优惠/newList/计算逻辑行为完全一致 |
| P2 | 逐任务验证 + 完整页面回归 | 汇总行、附件功能、父子通信行为完全一致 |
| P3 | 完整保存/提交流程回归 | 所有费用相关流程与重构前一致 |

---

## 十六、不会改动的部分

- 接口调用逻辑不变（postSaveWaybill、postWaybillDetails 等）
- 费用类型/费用项目的下拉选项数据来源不变
- 风控预警逻辑不变
- 亏吨弹窗逻辑不变
- 操作记录分页逻辑不变
- edit.vue 的保存/提交/风控流程整体结构不变（P2-4 仅改通信方式，不改流程）
- read.vue 仅受 P1-1（油气优惠收拢）间接影响，不做主动改动

---

## 十七、条件编译说明

本项目为 `logisticsweb`（Vue2 + JS + Element UI），不涉及多端差异，无需条件编译。

---

## 十八、与已有文档的关系

| 已有文档 | 本方案对应 | 说明 |
|---------|-----------|------|
| 重构落地方案（方案A-执行卡片）.md | 全部 | 本方案合并了方案A的全部 16 个 PR |
| 落地重构方案技术文档（方案B-技术方案）.md | 全部 | 本方案合并了方案B的全部 10 个任务 |
| components/CostMessageDetails.md | 全部 | 组件文档的 12 个重构风险点已全部覆盖 |
| 计算逻辑梳理与优化.md（方案 A~F） | P1-3/P0-1/P3-1/P1-2/P2-1 | 方案 A→P1-3, B→P0-1, C→P3-1, D→P1-2, E→P2-1 |

**本方案相对已有文档的增量**：
- 合并了方案A独有的 5 个 bug（B1/B4/B5/B6/B7）
- 合并了方案B独有的 2 个 bug（B8 computed v-model / B9 $forceUpdate）
- 合并了方案B独有的油气优惠逻辑收拢（P1-1，3处→1处）
- 合并了方案B独有的组件拆分（P2-2 汇总副表 / P2-3 附件管理）
- 统一了 newList 构建策略（方案A的消除双重初始化 + 方案B的 buildNewList 方法提取）

---

**文档版本**: v1.0（合并版）
**最后更新**: 2026年07月
**整理人**: 王新骏
**合并来源**: 重构落地方案（方案A-执行卡片）.md v2.0 + 落地重构方案技术文档（方案B-技术方案）.md
