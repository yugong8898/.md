# REQ-170 阶段3实施记录

## 目录
- [保证金关联改动回退](#保证金关联改动回退)
- [最小范围处理结果](#最小范围处理结果)
- [授权与范围](#授权与范围)
- [工程基线](#工程基线)
- [实施与验证结果](#实施与验证结果)
- [交付后编译阻塞修复](#交付后编译阻塞修复)
- [本地回归脚本](#本地回归脚本)

## 保证金关联改动回退

2026-09-24，用户确认“按照非必要不改动和最小范围调整的原则，回退相关修改”。本节为最新结果，下节保留此前按钮精简的历史记录。

- 仅回退 `buy.vue` 的保证金配套改动：恢复原 `v-model:visible`、boolean 类型、等待时解锁与选择后重锁；撤回关闭 handler、null 取消、对应卸载清理及 import。组件绑定、resolver、确认方法和保证金等待代码已通过HEAD一致性断言；未改子组件。
- Checkbox、接口布尔值、提交类型快照、创建及签约／支付分流保留；补取身份前的锁未改动，三个前置接口、原按钮及公共布局不变。
- 先建立回退断言，15组中2组预期失败；回退后15/15通过。3个绑定取消／卸载策略的用例转为历史留存，新增原逻辑一致性和两种保证金确认的快照回归；不宣称取消防重仍生效。
- ESLint `--no-ignore`：exit=0，0 error、3条历史warning；页面和布局保留注释的模板编译通过，既有Vite的购买页及Less模块均HTTP 200、JavaScript。IDE仅报告购买页原属性换行warning。
- 本次未重跑全量类型检查，沿用前轮104 errors／53 files的已接受基线限制；未执行真实下单、签约支付、完整构建或阶段5、6，未新增CodeReview。
- `git diff HEAD --check`通过，业务总差异为两文件、81行增加／19行删除。本次开始时两文件已有暂存内容，收尾检查时暂存区已为空、两文件均为未暂存修改。本次未执行任何暂存或取消暂存操作，也未提交、推送。
- 原保证金关闭仅隐藏弹窗、不完成等待的行为随之恢复；等待期间不保持提交锁，相关原有重复提交风险未修复。后续修复须先确认，不在本次增加交互。

## 最小范围处理结果

2026-09-24，用户要求“按照上述规则重新审视还未提交的这些代码，并按照规则处理”，并明确“理论上按钮不要任何改动呀”。本节为v1.3历史结果；保证金部分由上节回退记录接续，下文首次实施及接口布尔调整也不代表当前全部行为。

- 最终业务差异仅 `buy.vue`、`findService/types.ts` 两文件，109行增加、26行删除。`PortalLayout.vue` 整个文件与HEAD一致，本轮没有重新写入。
- 原提交按钮模板、文案优先级和 `submitDisabled` 表达式恢复HEAD；撤回“查看订单”和已有订单跳详情分支，不新增禁用、提示续办或本页重试策略。
- 撤回 `confirmPreviewReady`、`pageDisposed`、外层统一catch、合同调用新增await、卸载时价格版本递增、额外说明及样式。价格请求版本比较、两处DOM同步方法、合同状态方法均与HEAD一致。
- 保留单个Checkbox及必要样式；展示完全由接口布尔值决定，初始／缺失／异常为false，明确true才放行。允许且勾选时创建类型为2，否则保留入口类型；三个前置接口与原价格watch不变。
- 保留提交类型快照及提交期间防重：补取身份前加锁、保证金等待不解锁、关闭以null取消、finally释放。卸载仅清理已经挂起的保证金选择，不宣称阻断所有迟到请求。
- 恢复的是本次额外扩展，不删除原业务；必要替换仍用需求号注释保留原表达式。用户已清理的内容不加回，也不附带其他优化。

验证结果：
- 红绿回归：先更新脚本，16组中4组按预期失败；精简源码后16/16通过，exit=0。新断言验证原按钮和原方法不变，另覆盖选择、类型分流、三接口参数、保证金取消与保留注释的模板编译。
- 5个绑定已撤回策略的旧用例保留但不注册执行，必要行为由当前用例继续覆盖；旧“已有订单不重复创建”断言不再作为通过证据。
- ESLint `--no-ignore` 检查两个差异文件及布局：exit=0，0 error、3条历史warning。
- 既有3000端口Vite服务：购买页、布局的页面模块及Less模块均返回HTTP 200和JavaScript；未新建或重启服务。
- 重新执行 `vue-tsc --noEmit -p tsconfig.app.json --pretty false`：exit=2，104 errors／53 files，两个差异文件无诊断。统计与已接受基线一致，不声称全量类型检查通过；IDE诊断服务未就绪，以CLI为准。
- `git diff HEAD --check`：exit=0。未暂存、提交或推送；未执行真实下单、支付签约、浏览器验收、完整生产构建或正式阶段5、6。

本次用户新授权的单代理复审在精简前完成，未发现高置信度新增缺陷，并指出非必要扩展；精简后由主代理执行以上定向验证，没有再派审查代理。

遗留边界：原流程在订单已创建、支付失败或合同状态失败后再次提交，仍可能再次发起创建；创建响应丢失时的服务端幂等保障未验证。因此PRD网络异常重试不重复创建尚不能判定完整验收通过。本次按用户要求保持原按钮，不引入续办策略；如需修复，先确认方案和范围。

## 授权与范围

用户授权“执行3、4”，按已确认技术方案连续执行批次 A、B，完成一次审查后暂停。不暂存、提交、推送，不进入正式阶段5、6。

业务改动限于 `ai-platform/es-web`：
- `src/api/employer/findService/types.ts`
- `src/views/employer/purchase/buy.vue`
- `src/layouts/portal/PortalLayout.vue`

不改原始 PRD、R0、接口封装、三个前置接口、公共合同／支付组件、依赖、锁文件或知识库。工程回归脚本随本记录保存，不另建源码或测试框架。

<!-- 此节在v1.2时标题为“当前生效口径”；后续最小范围处理已由本文开头接续。 -->
### 接口布尔调整（历史记录）

用户要求展示完全由接口布尔值决定，缺失或异常默认false；在说明同路由query复用边界后，用户以“OK”确认“简化接口布尔判断，恢复layout原逻辑，不额外扩展路由重建逻辑”。本节覆盖历史方案中的前端组合条件及确认页专属key；原始PRD、R0和技术方案保留历史快照，不据此恢复旧实现。

- `buy.vue` 两处展示直接绑定 `canChangeOrderType`；初始、重新获取前、响应缺失或异常均为false，成功响应只接受 `res.data?.order?.canChangeOrderType === true`。不再叠加入口类型或需求ID条件。
- 创建类型仅在接口允许且勾选时为2，否则保留原入口类型；原需求类型2不因未勾选降为1。三个下单前接口、提交快照、保证金取消及防重复提交保持不变。
- `PortalLayout.vue` 恢复 `resolveRouterViewCacheKey(viewRoute.path)`，运行逻辑与原布局一致，仅保留撤回方案的历史注释。确认页内只更换query不额外重建，也不增加页面局部路由监听；相关旧状态复用风险继续沿用原行为，不作为本次已修复项。
- 回归按新口径更新：改代码前15组中3组失败，分别暴露旧组合条件、模板绑定和fullPath key；修改后15/15通过。缺失order、缺失data、业务失败及请求异常都从已有true重置为false；旧断言注释留存，不再执行。
- 验证：ESLint `--no-ignore` 为0 error、3条历史警告；既有3000端口Vite服务的布局与购买页模块均返回HTTP 200及JavaScript。保留注释的两份模板编译通过，不关闭错误浮层。
- 本轮未重跑全量vue-tsc；IDE仍报告布局636行历史未定义引用，此前104项历史错误的门禁限制保留。未执行真实业务或全量页面验收；没有第二次CodeReview，当前结果为主代理定向验证。

## 工程基线

- 日期：2026-09-24；分支：`F-REQ-170-wxj`；HEAD：`691ac78e576499034c65eccf46e965aa7c3539bf`。编码前工作区及暂存区均无改动。
- Node v22.22.2；ESLint v9.39.4；TypeScript v5.7.3；使用本地依赖。
<!-- 初次记录：三目标文件 ESLint --quiet 返回0，后来发现文件被忽略，该结果无效。 -->
- 初次 ESLint `--quiet` 返回0但实际忽略了文件，不作为通过证据。改用项目 ESLint API `ignore:false` 强制校验，再以 `git show HEAD:<file>` 配合 `lintText` 检查原始源码：基线为0 error、3 warnings（buy.vue 属性换行1条，PortalLayout 未使用 import 2条）。
- `vue-tsc --noEmit -p tsconfig.app.json`：exit=2，104 errors in 53 files。包括 `PortalLayout.vue:636` 的 `persistNologinConversationIdIfGuest` 未定义；buy.vue 和 findService/types.ts 未出现在诊断中。
- 用户在基线失败确认中选择“记录基线后继续”：仅修本次新增问题，全量类型检查继续如实标记未通过。
- 不运行仅扫描暂存区的 quick-verify，不以空暂存区伪造门禁通过。

## 实施与验证结果

以下保留首次交付的验证记录；其中布局开发态编译存在漏检，已由用户反馈发现并修复，见“交付后编译阻塞修复”。原12组通过不代表当时布局可正常编译。

两个批次的业务代码已完成。按用户接受的历史错误基线继续审查；全量类型门禁未通过，不能标为全部验证通过。

| 批次 | 实际内容 | 验证 |
|---|---|---|
| A | 可选预览权限类型、局部 checkbox 与严格权限、提交类型快照、全程锁、保证金关闭取消、已创建订单查看入口、卸载保护、临时 UI | 本地回归11组通过，尚待批次B的路由 key 断言1组失败；ESLint 强制校验复核0 error；类型诊断未新增 |
| B | 仅 EmployerPurchaseBuy 的组件 key 使用 fullPath；其他页面保留 path helper | 本地回归12组全部通过，exit=0；三文件 ESLint 强制校验0 error、3条基线警告 |

红绿验证：编码前12组中11组失败（缺少选择状态、重复创建、加锁过晚、关闭未完成等待、缺少专属 key），1组原逻辑保护通过；批次A后只剩 key 断言失败；批次B后12/12通过。脚本执行真实页面逻辑，但网络和认证均为替身，不代表正式阶段5/6测试。

最终工程检查：
- `git diff --check`：exit=0；业务 diff 仅3个既定文件，173行增加、32行删除。删除计数主要为移位或表达式替换；替换的旧表达式／绑定通过 `[REQ-170]` 注释保留，未清理无关业务。
- 实际 ESLint 校验（`ignore:false`，等效 CLI `--no-ignore`）：0 error；buy.vue 1条、PortalLayout 2条警告，与 HEAD 基线规则及数量一致；types.ts 无警告。
- `vue-tsc --noEmit -p tsconfig.app.json --pretty false`：exit=2。按文件、行列、错误码和主消息归一化比对基线：before=104、after=104、added=[]、removed=[]。未声称全量类型检查通过。
- SFC script、template 及 Less 独立编译通过；未执行完整生产构建。
- 编辑器诊断服务未就绪；以实际 CLI 校验为准。
- 三前置接口的参数构造和价格 watch 已通过源码基线断言，未新增日志、接口、依赖或持久化字段。

源码范围外变更：无。文档仅本实施记录、同级审查报告与 workflow-state.md；未进入正式阶段5、6。

未执行：真实接口联调、真实签约／支付、浏览器键盘与视觉验收、全量页面回归。阶段4审查结论另见同级报告，不将本地脚本通过等同于可上线。

主要源码定位：
- buy.vue：权限与选择状态约156–174行；预览约775–823行；保证金取消约978–986行；提交约1028–1258行；卸载约1473–1483行；UI约1810–1842行；Less约1980行起。
- types.ts：预览响应206–211行。
- PortalLayout.vue：确认页专属 key 1763–1767行。

可复跑的回归命令（工作区根目录）：

```bash
node -e 'const fs=require("node:fs");const p=".md/需求/REQ-170 原直采模式支持雇主购买前选择购买模式-PRD/实施记录-REQ-170.md";eval(fs.readFileSync(p,"utf8").split("```javascript\n")[1].split("\n```")[0])'
```

下方脚本用于本地开发回归，不代表正式测试基线、真实接口联调或验收通过。

## 交付后编译阻塞修复

- 2026-09-24：用户反馈 Vite 报错 `<KeepAlive> expects exactly one child component.`，随后明确授权“修复”。
- 根因：本次新增的 REQ-170 注释放在 `keep-alive` 内部；保留注释编译时，注释与动态组件构成两个子节点。这是本次引入的编译阻塞，不属于104项历史类型错误。
- 漏检：原脚本只对 buy.vue 做模板编译；布局先去除注释再验证 key 表达式，没有覆盖保留注释的完整布局模板。此前“独立编译通过”不能用于证明布局开发态可运行。
- 修复：注释原样移至 `keep-alive` 外侧；key、缓存名单、路由及其他业务逻辑均不变。横向检查4处 `keep-alive`，其余3处无相同问题。
- 红绿证据：对完整布局执行 `compilerOptions: { comments: true }` 编译断言，修复前 exit=1，错误与用户反馈一致；外移注释后同一断言 exit=0。原12组回归重跑全部通过；新增断言合入下方脚本。
- ESLint 强制校验仍为0 error、3条历史警告。本轮未重跑全量 vue-tsc，保留此前104项历史错误记录；未启动正式阶段5/6、未重新派遣 CodeReview，也未关闭 Vite 错误浮层。
- 本节记录的是定向编译与本地回归，不代表真实接口、支付签约、浏览器视觉或全量页面验收通过。

## 本地回归脚本

使用 Node 内置断言、项目已有 TypeScript 与 Vue；直接解析并执行 buy.vue 的真实 script setup，网络、认证和导航隔离为测试替身，不访问业务环境。模板及路由 key 做编译／结构验证；不覆盖真实浏览器点击、布局、键盘及公共收银台内部行为。

执行方式：在工作区根目录运行 Node，提取本节唯一 `javascript` 代码块后执行。脚本不写文件，退出码非零表示至少一项失败。

```javascript
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');
const assert = require('node:assert/strict');
const { execFileSync } = require('node:child_process');
const root = path.resolve('ai-platform/es-web');
const ts = require(path.join(root, 'node_modules/typescript'));
const vue = require(path.join(root, 'node_modules/vue'));
const sfc = require(path.join(root, 'node_modules/vue/compiler-sfc'));
const sourcePath = 'src/views/employer/purchase/buy.vue';
const source = fs.readFileSync(path.join(root, sourcePath), 'utf8');
const parsed = sfc.parse(source);
assert.equal(parsed.errors.length, 0);
const script = parsed.descriptor.scriptSetup.content;
const ast = ts.createSourceFile('buy.ts', script, ts.ScriptTarget.Latest, true, ts.ScriptKind.TS);
const statements = ast.statements.filter(n => !ts.isImportDeclaration(n));
const names = statements.flatMap(n => ts.isFunctionDeclaration(n) ? [n.name.text] : ts.isVariableStatement(n) ? n.declarationList.declarations.filter(d => ts.isIdentifier(d.name)).map(d => d.name.text) : []);
const executable = ts.transpileModule(statements.map(n => n.getText(ast)).join('\n').replaceAll('import.meta.env.VITE_DISABLE_SUBMIT_ORDER', 'false') + '\n;globalThis.state = {' + names.join(',') + '};', { compilerOptions: { target: ts.ScriptTarget.ES2022, module: ts.ModuleKind.CommonJS } }).outputText;
const noop = () => {};
const deferred = () => { let resolve; let reject; const promise = new Promise((a, b) => { resolve = a; reject = b; }); return { promise, resolve, reject }; };
const tick = () => new Promise(resolve => setImmediate(resolve));
function setup(query = {}, overrides = {}) {
  const calls = { create: [], match: [], coupon: [], price: [], sign: [], pay: [], nav: [], track: [], error: [] };
  const hooks = [];
  const scopes = vue.effectScope();
  const user = vue.reactive({ employerId: 'employer-test', fetchUserInfo: async () => {} });
  const route = vue.reactive({ query: { productId: 'product-test', orderType: '1', ...query } });
  const product = { status: 1, plans: [{ type: 'base', stock: 9, price: 10 }] };
  const ctx = {
    ref: vue.ref, computed: vue.computed, watch: vue.watch, nextTick: vue.nextTick,
    onMounted: noop, onUnmounted: fn => hooks.push(fn), onBeforeUnmount: fn => hooks.push(fn),
    useRoute: () => route, useRouter: () => ({ push: x => calls.nav.push(x), replace: x => calls.nav.push(x) }),
    useUserStore: () => user, useServerImSender: () => ({ sendMessage: noop }),
    useAplus: () => ({ aplusCustom: noop, aplusExposure: x => calls.track.push(x), aplusPv: noop, endPageTrackingPv: noop }),
    trackClk: x => calls.track.push(x), getEmployerTrackingUserId: () => 'employer-test', formatDateTime: () => '',
    message: { error: x => calls.error.push(x), success: noop, warning: noop },
    console: { log: noop, warn: noop }, document: {}, window: { open: x => calls.nav.push(x) },
    requireCert: () => true, ensureEmployerFeatureAvailable: async () => true,
    getEmployerOrderConfirmPreview: async () => ({ ok: true, data: { product, order: { canChangeOrderType: true } } }),
    postEmployerCouponPrecalculate: async x => { calls.coupon.push(x); return { ok: true, data: {} }; },
    postEmployerReviewPrice: async x => { calls.price.push(x); return { ok: true, data: { totalPayAmount: '10' } }; },
    matchFulfillmentBond: async x => { calls.match.push(x); return { ok: true, data: { matched: false } }; },
    createEmployerOrderV2: async x => { calls.create.push(x); return { ok: true, data: { id: 'order-test', payAmount: '10' } }; },
    getEmployerContractBriefStatus: async id => { calls.sign.push(id); return { ok: false }; },
    openEmployerOrderCashier: async (...args) => { calls.pay.push(args); args[3].onCashierOpen(); return 'failed'; },
    getCouponsForProductTier: () => [], formatBestCouponBracketTag: () => '', resolveCouponRealDiscountAmount: () => 0,
    FulfillmentModeEnum: {}, ContractStatus: {}, PRICE_UNIT_LABEL: {}, PayResultStatus: { Failed: 'failed' },
    resolvePaymentStandardCardVariant: () => '', resolveEmployerOrderPayOrderType: x => x, resolveEmployerOrderPayEntrySource: x => x,
    EMPLOYER_COUPON_TRIGGER_TYPE: { PAY_SUCCESS: 1 }, requestEmployerCouponTriggerPopup: noop,
    backOrEmployerServiceList: () => calls.nav.push('back'), clearTimeout, setTimeout,
    ...overrides,
  };
  scopes.run(() => vm.runInNewContext(executable, ctx));
  const state = ctx.state;
  return { state, ctx, calls, user, route, product, dispose: () => { hooks.forEach(fn => fn()); scopes.stop(); } };
}
async function ready(query = {}, overrides = {}) { const h = setup(query, overrides); await h.state.fetchDetail(); await tick(); return h; }
const tests = [];
// 用户要求原按钮与原异步流程不改，以下旧策略用例仅留作历史，不再注册执行。
const retiredTests = new Set([
  '选中仅改变创建及签约，不改变前置试算或保证金',
  '未选直采仍走收银台，支付失败不重复创建',
  '预览失败／创建失败刷新清除旧权限与选择',
  '卸载终止保证金等待及迟到预览',
  '无预览／加载中不可创建，已有订单可查看',
  // 用户确认回退保证金取消与等待锁，旧断言仅作历史留存。
  '保证金关闭取消，不释放等待中的锁，不创建且可重试',
  '保证金需要保持参数，等待后创建类型使用快照',
  '卸载仅清理尚未完成的保证金选择',
]);
const test = (name, run) => { if (!retiredTests.has(name)) tests.push([name, run]); };
test('权限严格布尔、默认未选、取消与重新勾选', async () => {
  for (const permission of [true, false, undefined, null, 'true', 1]) {
    const h = setup();
    h.ctx.getEmployerOrderConfirmPreview = async () => ({ ok: true, data: { product: h.product, order: { canChangeOrderType: permission } } });
    await h.state.fetchDetail();
    // [REQ-170] 原校验 showOrderTypeOption；改为直接使用接口布尔值。
    assert.equal(h.state.canChangeOrderType.value, permission === true);
    assert.equal(h.state.signBeforePay?.value, false);
    h.state.signBeforePay.value = true;
    assert.equal(h.state.createOrderType.value, permission === true ? 2 : 1);
    h.state.signBeforePay.value = false;
    assert.equal(h.state.createOrderType.value, 1);
    h.dispose();
  }
});
/* [REQ-170] 用户改为接口统一判断，保留原需求链路断言供追溯。
test('需求链路与异常混合链接不展示', async () => {
  for (const [query, type] of [[{ orderType: '2', demandId: 'demand-test' }, 2], [{ demandId: 'demand-test' }, 1]]) {
    const h = await ready(query);
    assert.equal(h.state.showOrderTypeOption?.value, false);
    h.state.signBeforePay.value = true;
    assert.equal(h.state.createOrderType.value, type);
    h.dispose();
  }
});
*/
test('入口类型和需求ID不覆盖接口布尔值，未选保留原类型', async () => {
  for (const [query, type] of [[{}, 1], [{ orderType: '2', demandId: 'demand-test' }, 2], [{ demandId: 'demand-test' }, 1]]) {
    for (const permission of [true, false]) {
      const h = setup(query);
      h.ctx.getEmployerOrderConfirmPreview = async () => ({ ok: true, data: { product: h.product, order: { canChangeOrderType: permission } } });
      await h.state.fetchDetail();
      assert.equal(h.state.canChangeOrderType.value, permission);
      assert.equal(h.state.createOrderType.value, type);
      h.state.signBeforePay.value = true;
      assert.equal(h.state.createOrderType.value, permission ? 2 : type);
      h.dispose();
    }
  }
});
test('接口字段缺失或请求异常时权限默认false，不残留上次true', async () => {
  for (const mode of ['missing-order', 'missing-data', 'failed', 'exception']) {
    const h = setup();
    assert.equal(h.state.canChangeOrderType.value, false);
    await h.state.fetchDetail();
    assert.equal(h.state.canChangeOrderType.value, true);
    h.ctx.getEmployerOrderConfirmPreview = async () => {
      if (mode === 'exception') throw Error('offline');
      if (mode === 'missing-data') return { ok: true };
      return { ok: mode !== 'failed', data: { product: h.product, ...(mode === 'failed' ? { order: { canChangeOrderType: true } } : {}) } };
    };
    await h.state.fetchDetail();
    assert.equal(h.state.canChangeOrderType.value, false);
    h.dispose();
  }
});
test('选中仅改变创建及签约，不改变前置试算或保证金', async () => {
  const h = await ready();
  assert.ok(h.state.signBeforePay, '缺少模式选择状态');
  const count = h.calls.coupon.length + h.calls.price.length;
  h.state.signBeforePay.value = true;
  await tick();
  assert.equal(h.calls.coupon.length + h.calls.price.length, count);
  assert.equal(h.state.buildReviewPriceParams('employer-test').orderType, 1);
  await h.state.handleSubmitOrder();
  assert.equal(h.calls.match[0].orderType, 1);
  assert.equal(h.calls.create[0].orderType, 2);
  assert.equal(h.calls.create[0].demandId, undefined);
  assert.equal(h.calls.sign.length, 1);
  assert.equal(h.calls.pay.length, 0);
  assert.equal(h.calls.track.find(x => x.eid === 'order_submit_btn').params.order_type, '2');
  await h.state.handleSubmitOrder();
  assert.equal(h.calls.create.length, 1);
  assert.equal(h.state.submitButtonText?.value, '查看订单');
  h.dispose();
});
test('未选直采仍走收银台，支付失败不重复创建', async () => {
  const h = await ready({ from: 'im' });
  await h.state.handleSubmitOrder();
  assert.equal(h.calls.create[0].orderType, 1);
  assert.equal(h.calls.pay.length, 1);
  assert.equal(h.calls.sign.length, 0);
  assert.equal(h.calls.track.find(x => x.eid === 'pay_channel_show').entry_source, 'im');
  await h.state.handleSubmitOrder();
  assert.equal(h.calls.create.length, 1);
  h.dispose();
});
test('补取身份前加锁，连点只有一个事务', async () => {
  const gate = deferred(); const h = await ready();
  h.user.employerId = ''; let fetches = 0;
  h.user.fetchUserInfo = async () => { fetches++; await gate.promise; h.user.employerId = 'employer-test'; };
  const pending = h.state.handleSubmitOrder();
  assert.equal(h.state.submitting.value, true);
  await h.state.handleSubmitOrder();
  assert.equal(fetches, 1);
  gate.resolve(); await pending;
  assert.equal(h.calls.create.length, 1);
  h.dispose();
});
test('保证金关闭取消，不释放等待中的锁，不创建且可重试', async () => {
  const h = await ready({}, { matchFulfillmentBond: async () => ({ ok: true, data: { matched: true, bondAmount: '10', ruleNo: 'r' } }) });
  const pending = h.state.handleSubmitOrder(); await tick();
  assert.equal(h.state.submitting.value, true);
  assert.equal(typeof h.state.handleBondVisibleChange, 'function');
  h.state.handleBondVisibleChange(false); await pending;
  assert.equal(h.calls.create.length, 0);
  assert.equal(h.state.submitting.value, false);
  assert.equal(h.state.bondConfirmResolve.value, null);
  const again = h.state.handleSubmitOrder(); await tick();
  h.state.handleBondConfirm(false); h.state.handleBondVisibleChange(false); await again;
  assert.equal(h.calls.create.length, 1);
  assert.equal(h.calls.create[0].needFulfillmentBond, undefined);
  h.dispose();
});
test('保证金需要保持参数，等待后创建类型使用快照', async () => {
  const h = await ready({}, { matchFulfillmentBond: async () => ({ ok: true, data: { matched: true, bondAmount: '10', ruleNo: 'r' } }) });
  assert.ok(h.state.signBeforePay, '缺少模式选择状态');
  h.state.signBeforePay.value = true;
  const pending = h.state.handleSubmitOrder(); await tick();
  h.state.signBeforePay.value = false;
  h.state.handleBondConfirm(true); h.state.handleBondVisibleChange(false); await pending;
  assert.equal(h.calls.create[0].orderType, 2);
  assert.equal(h.calls.create[0].needFulfillmentBond, true);
  assert.equal(h.calls.create[0].bondAmount, '10');
  assert.equal(h.calls.sign.length, 1);
  h.dispose();
});
test('预览失败／创建失败刷新清除旧权限与选择', async () => {
  const h = await ready(); assert.ok(h.state.signBeforePay, '缺少模式选择状态');
  h.state.signBeforePay.value = true;
  h.ctx.createEmployerOrderV2 = async () => ({ ok: false, message: '拒绝' });
  await h.state.handleSubmitOrder();
  assert.equal(h.state.signBeforePay.value, false);
  assert.equal(h.state.currentOrderId.value, '');
  h.ctx.getEmployerOrderConfirmPreview = async () => { throw Error('offline'); };
  await h.state.fetchDetail();
  assert.equal(h.state.confirmPreviewReady.value, false);
  // [REQ-170] 原 assert.equal(h.state.showOrderTypeOption.value, false);
  assert.equal(h.state.canChangeOrderType.value, false);
  assert.equal(h.state.submitDisabled.value, true);
  h.dispose();
});
test('卸载终止保证金等待及迟到预览', async () => {
  const h = await ready({}, { matchFulfillmentBond: async () => ({ ok: true, data: { matched: true, bondAmount: '10' } }) });
  const pending = h.state.handleSubmitOrder(); await tick(); h.dispose();
  assert.equal(h.state.bondConfirmResolve.value, null);
  await pending; assert.equal(h.calls.create.length, 0);
  const gate = deferred(); const other = setup({}, { getEmployerOrderConfirmPreview: () => gate.promise });
  const preview = other.state.fetchDetail(); other.dispose();
  gate.resolve({ ok: true, data: { product: other.product, order: { canChangeOrderType: true } } });
  await preview;
  assert.equal(other.state.detail.value, null);
});
test('无预览／加载中不可创建，已有订单可查看', async () => {
  const h = setup();
  await h.state.handleSubmitOrder(); assert.equal(h.calls.create.length, 0);
  await h.state.fetchDetail(); h.state.loading.value = true;
  await h.state.handleSubmitOrder(); assert.equal(h.calls.create.length, 0);
  h.state.currentOrderId.value = 'existing';
  assert.equal(h.state.submitDisabled.value, false);
  await h.state.handleSubmitOrder(); assert.equal(h.calls.create.length, 0);
  assert.equal(h.calls.nav.at(-1).params.orderId, 'existing'); h.dispose();
});
test('原提交按钮模板和禁用表达式与HEAD完全一致', () => {
  const old = execFileSync('git', ['show', 'HEAD:' + sourcePath], { cwd: root, encoding: 'utf8' });
  const button = text => text.match(/<!-- 提交订单按钮 -->[\s\S]*?<\/button>/)[0];
  const disabled = text => text.match(/const submitDisabled = computed\([\s\S]*?\n/)[0];
  assert.equal(button(source), button(old));
  assert.equal(disabled(source), disabled(old));
});
test('价格、DOM同步、合同方法及公共布局恢复HEAD', () => {
  const old = execFileSync('git', ['show', 'HEAD:' + sourcePath], { cwd: root, encoding: 'utf8' });
  const block = (text, name) => {
    const sf = ts.createSourceFile('x.ts', sfc.parse(text).descriptor.scriptSetup.content, ts.ScriptTarget.Latest, true);
    return sf.statements.find(n => ts.isFunctionDeclaration(n) && n.name.text === name).getText(sf);
  };
  for (const name of ['isLatestPriceDataRequest', 'initialProductInfoTextWrapSync', 'attachProductInfoLayoutObserver', 'handleSignContract']) {
    assert.equal(block(source, name), block(old, name), name);
  }
  const filename = 'src/layouts/portal/PortalLayout.vue';
  assert.equal(fs.readFileSync(path.join(root, filename), 'utf8'), execFileSync('git', ['show', 'HEAD:' + filename], { cwd: root, encoding: 'utf8' }));
});
test('不新增按钮用途、预览门禁或全链卸载状态', () => {
  assert.ok(!names.includes('submitButtonText'));
  assert.ok(!names.includes('confirmPreviewReady'));
  assert.ok(!names.includes('pageDisposed'));
  const fn = ast.statements.find(n => ts.isFunctionDeclaration(n) && n.name.text === 'handleSubmitOrder').getText(ast);
  assert.ok(!/if\s*\(currentOrderId\.value\)/.test(fn));
  assert.ok(fn.includes('void handleSignContract(orderId)'));
  assert.ok(!fn.includes('await handleSignContract(orderId)'));
  const template = parsed.descriptor.template.content.replace(/<!--[\s\S]*?-->/g, '');
  assert.ok(!template.includes('order-type-note'));
});
test('勾选控制创建和签约分流，未勾选仍走原收银台', async () => {
  for (const selected of [false, true]) {
    const h = await ready({ from: 'im' });
    const count = h.calls.coupon.length + h.calls.price.length;
    h.state.signBeforePay.value = selected;
    await tick();
    assert.equal(h.calls.coupon.length + h.calls.price.length, count);
    await h.state.handleSubmitOrder();
    assert.equal(h.calls.match[0].orderType, 1);
    assert.equal(h.calls.create[0].orderType, selected ? 2 : 1);
    assert.equal(h.calls.sign.length, selected ? 1 : 0);
    assert.equal(h.calls.pay.length, selected ? 0 : 1);
    assert.equal(h.calls.track.find(x => x.eid === 'order_submit_btn').params.order_type, selected ? '2' : '1');
    if (!selected) assert.equal(h.calls.track.find(x => x.eid === 'pay_channel_show').entry_source, 'im');
    h.dispose();
  }
});
test('创建失败刷新及预览异常仍清除权限与选择', async () => {
  const h = await ready();
  h.state.signBeforePay.value = true;
  h.ctx.createEmployerOrderV2 = async () => ({ ok: false, message: '拒绝' });
  await h.state.handleSubmitOrder();
  assert.equal(h.state.signBeforePay.value, false);
  assert.equal(h.state.currentOrderId.value, '');
  h.ctx.getEmployerOrderConfirmPreview = async () => { throw Error('offline'); };
  await h.state.fetchDetail();
  assert.equal(h.state.canChangeOrderType.value, false);
  assert.equal(h.state.signBeforePay.value, false);
  h.dispose();
});
test('卸载仅清理尚未完成的保证金选择', async () => {
  const h = await ready({}, { matchFulfillmentBond: async () => ({ ok: true, data: { matched: true, bondAmount: '10' } }) });
  const pending = h.state.handleSubmitOrder();
  await tick();
  h.dispose();
  await pending;
  assert.equal(h.state.bondConfirmResolve.value, null);
  assert.equal(h.calls.create.length, 0);
  assert.equal(h.state.submitting.value, false);
});
test('保证金绑定、类型、方法及等待流程恢复HEAD', () => {
  const old = execFileSync('git', ['show', 'HEAD:' + sourcePath], { cwd: root, encoding: 'utf8' });
  const block = (text, name) => {
    const sf = ts.createSourceFile('x.ts', sfc.parse(text).descriptor.scriptSetup.content, ts.ScriptTarget.Latest, true);
    return sf.statements.find(n => ts.isFunctionDeclaration(n) && n.name.text === name).getText(sf);
  };
  const dialog = text => text.match(/    <BondRequiredDialog[\s\S]*?    \/>/)[0];
  const resolver = text => text.match(/const bondConfirmResolve = [^\n]+/)[0];
  const waiting = text => text.split('    let needFulfillmentBondForTracking = false')[1].split('    // 3. 创建订单')[0];
  assert.equal(dialog(source), dialog(old));
  assert.equal(resolver(source), resolver(old));
  for (const name of ['requestBondConfirmation', 'handleBondConfirm']) assert.equal(block(source, name), block(old, name));
  assert.equal(waiting(source), waiting(old));
  assert.ok(!names.includes('handleBondVisibleChange'));
  assert.ok(!source.includes('onBeforeUnmount'));
});
test('保证金沿用原等待锁与两种确认，创建类型仍使用快照', async () => {
  for (const required of [false, true]) {
    const h = await ready({}, { matchFulfillmentBond: async () => ({ ok: true, data: { matched: true, bondAmount: '10', ruleNo: 'r' } }) });
    h.state.signBeforePay.value = true;
    const pending = h.state.handleSubmitOrder();
    await tick();
    assert.equal(h.state.submitting.value, false);
    assert.equal(h.calls.create.length, 0);
    h.state.signBeforePay.value = false;
    h.state.handleBondConfirm(required);
    h.state.bondDialogVisible.value = false;
    await pending;
    assert.equal(h.calls.create.length, 1);
    assert.equal(h.calls.create[0].orderType, 2);
    assert.equal(h.calls.create[0].needFulfillmentBond, required ? true : undefined);
    assert.equal(h.calls.create[0].bondAmount, required ? '10' : undefined);
    assert.equal(h.calls.create[0].bondRuleNo, required ? 'r' : undefined);
    assert.equal(h.state.submitting.value, false);
    assert.equal(h.calls.sign.length, 1);
    h.dispose();
  }
});
test('三接口参数构造与价格 watch 保持源码基线', () => {
  const old = execFileSync('git', ['show', 'HEAD:' + sourcePath], { cwd: root, encoding: 'utf8' });
  for (const name of ['buildConfirmPreviewParams', 'buildReviewPriceParams', 'fetchCouponPrecalculate']) {
    const block = text => { const sf = ts.createSourceFile('x.ts', sfc.parse(text).descriptor.scriptSetup.content, ts.ScriptTarget.Latest, true); return sf.statements.find(n => ts.isFunctionDeclaration(n) && n.name.text === name).getText(sf); };
    assert.equal(block(source), block(old), name);
  }
  const matchBody = text => text.match(/const matchParams:[\s\S]*?\n    }/)[0];
  assert.equal(matchBody(source), matchBody(old));
  const priceWatch = text => text.match(/watch\(\s*\(\) => \[\s*quantity.value,[\s\S]*?\n\)/)[0];
  assert.equal(priceWatch(source), priceWatch(old));
});
// [REQ-170] 原同时验证确认页 fullPath key；现恢复原布局行为并单独断言。
test('模板编译、checkbox 位置及接口布尔值直接控制展示', () => {
  const result = sfc.compileTemplate({ source: parsed.descriptor.template.content, filename: 'buy.vue', id: 'req170', compilerOptions: { comments: true } });
  assert.equal(result.errors.length, 0);
  const template = parsed.descriptor.template.content.replace(/<!--[\s\S]*?-->/g, '');
  assert.ok(template.includes('v-model:checked="signBeforePay"'));
  // 原为两处展示；用户要求撤回按钮下额外说明，现仅保留Checkbox区域。
  assert.equal((template.match(/v-if="canChangeOrderType"/g) || []).length, 1);
  assert.ok(!template.includes('v-if="showOrderTypeOption"'));
  assert.ok(template.indexOf('need-pay-row') < template.indexOf('order-type-option'));
  assert.ok(template.indexOf('order-type-option') < template.indexOf('class="submit-btn"'));
});
test('布局恢复原path key，不因确认页query改变而重建', () => {
  const layout = fs.readFileSync(path.join(root, 'src/layouts/portal/PortalLayout.vue'), 'utf8').replace(/<!--[\s\S]*?-->/g, '');
  const expression = layout.match(/:key="([^"]*resolveRouterViewCacheKey[^\"]*)"/)[1];
  const key = route => vm.runInNewContext(expression, { viewRoute: route, resolveRouterViewCacheKey: x => 'legacy:' + x });
  // [REQ-170] 原期望 '/buy?productId=2'，用户确认恢复原 key。
  assert.equal(key({ name: 'EmployerPurchaseBuy', path: '/buy', fullPath: '/buy?productId=2' }), 'legacy:/buy');
  assert.equal(key({ name: 'EmployerPurchaseBuy', path: '/buy', fullPath: '/buy?productId=3' }), 'legacy:/buy');
  assert.equal(key({ name: 'Other', path: '/list', fullPath: '/list?q=2' }), 'legacy:/list');
});
test('布局模板保留注释时可编译，KeepAlive 保持单子节点', () => {
  const filename = 'src/layouts/portal/PortalLayout.vue';
  const layout = sfc.parse(fs.readFileSync(path.join(root, filename), 'utf8'));
  assert.deepEqual(layout.errors, []);
  const result = sfc.compileTemplate({
    source: layout.descriptor.template.content,
    filename,
    id: 'req170-layout',
    compilerOptions: { comments: true },
  });
  assert.deepEqual(result.errors.map(error => error.message), [], '布局模板保留注释时应编译通过');
});
(async () => {
  let failures = 0;
  for (const [name, run] of tests) { try { await run(); console.log('PASS', name); } catch (error) { failures++; console.error('FAIL', name, error.message); } }
  console.log(JSON.stringify({ total: tests.length, failures }));
  process.exitCode = failures ? 1 : 0;
})();
```

---
**文档版本**：v1.4  
**最后更新**：2026年09月  
**整理人**：王新骏
