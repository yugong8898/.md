# 【C146.9】现有实现确认 & TODO/待确认项报告

> 基于需求文档、改动清单与实际代码的逐项比对，整理三个模块的实现进度与剩余待处理事项。
> 最近一次代码核查日期：2026年7月
> 产品确认（2026年7月）：`appStatus` 和 `payStatusList` 同时传给服务端即可，前端无需处理优先级。

---

## 一、总体进度速览

**前端交互三个模块均已完成**，剩余工作只有服务端字段联调确认及少量前端收尾。

| 模块 | 前端交互完成度 | 剩余工作 |
|------|-------------|---------|
| 运单结算（settlement.vue） | ✅ 100% | 无，直接联调 |
| 运单付款（carrierWaybillList.vue） | ✅ 100% | 无前端工作，直接联调 |
| 付款申请记录（payManage.vue） | ✅ 100% | 少量前端收尾 + 需产品/后端确认 |

---

## 二、运单结算（settlement.vue）

### 已完成

| 改动点 | 状态 | 说明 |
|--------|------|------|
| 单据状态多选 | ✅ | `searchPopup.vue` 中 `busStatusBtns` 支持多选，`handleBusStatusClick` 实现"全部"互斥 |
| "全部"选项 | ✅ | `BUS_STATUS_OPTIONS` 含"全部"枚举 |
| Tab 联动 | ✅ | `change()` 调用 `calculateTabSwitchStatus` 取交集 |
| 筛选提交跳转"全部"Tab | ✅ | `handleSearch()` 中 `this.current = 0` |
| 切 Tab 同步筛选弹窗 | ✅ | `change()` 调用 `this.$refs.SearchPopup.setBusStatus()` |
| 不选状态→查全部 | ✅ | `getData()` 中 `delete filterModel.busStatusList` |
| 字段 `busStatusList` | ✅ 已验证 | logisticsweb 多处使用，后端支持数组类型 |

### 剩余工作

无。**可直接联调。**

---

## 三、运单付款（carrierWaybillList.vue）

> 说明：运单付款的 Tab 是「单笔/批量」切换，**不是状态 Tab**，因此需求中的 Tab-状态联动规则不适用于此模块。

### 已完成

| 改动点 | 状态 | 说明 |
|--------|------|------|
| 支付状态多选 | ✅ | `minePages/components/searchPopup.vue` 中 `payStatusBtns` 支持多选，`handlePayStatusClick` 实现互斥 |
| "全部"选项 | ✅ | `PAY_STATUS_OPTIONS` 含"全部"，`waybillPaymentConfig.js` 完整定义 |
| 默认值"全部"选中 | ✅ | `initButtonOptions` 使"全部"默认 active |
| 旧字段清理 | ✅ | `buildParams()` 中 `delete apiSearchParams.payStatus`，改用 `payStatusList` |
| 筛选弹窗回显 | ✅ | `initSearch()` 中根据 `payStatusList` 回显多选状态 |
| 字段 `payStatusList` | ✅ 已验证 | logisticsweb `freightNew`、`threePartyWaybillPayment` 等多处使用 |

### 剩余工作

无前端工作。**可直接联调。**

---

## 四、付款申请记录（payManage.vue）

### 已完成

| 改动点 | 状态 | 说明 |
|--------|------|------|
| 新增"全部"Tab 且为默认 | ✅ | `TAB_CONFIG[0]` 为"全部"，`current: 0` |
| 独立筛选弹窗 | ✅ | `payManage/components/searchPopup.vue` 已从共用组件拆分 |
| 删除"对账状态"和"支付状态" | ✅ | 独立弹窗中无这两个筛选项 |
| 新增"付款状态"多选（10个枚举值） | ✅ | `PAY_MANAGE_STATUS_OPTIONS` 含全部10个枚举，`handlePayStatusClick` 实现互斥 |
| Tab-状态映射表 | ✅ | 4个 Tab 映射定义在 `TAB_CONFIG` |
| Tab 联动 | ✅ | `tabChange()` 调用 `calculateTabSwitchStatus` |
| 筛选提交跳转"全部"Tab | ✅ | `handleSearch()` 中 `this.current = 0` |
| 字段 `payStatusList` | ✅ 已验证 | logisticsweb 三个页面使用同一接口且传 `payStatusList` |

### 剩余工作

#### 前端可直接处理

| 编号 | 事项 | 说明 |
|------|------|------|
| P-1 | Tab 切换后补调 `this.$refs.searchPopup.initSearch()` | `tabChange` 中已更新 `popupSearchConfig.payStatusList`，但未调用 `initSearch()` 同步弹窗 UI，导致切 Tab 后再打开弹窗时选中状态不刷新 |
| P-2 | 移除 12 处 `payStatusList` TODO 注释 | `payManage.vue`(4处)、`searchPopup.vue`(多处)、`payManageConfig.js`(2处)，字段名已被 logisticsweb 验证有效 |

#### 已确认待处理（产品/需求文档已给出结论）

| 编号 | 结论 | 当前代码状态 | 需要的代码改动 |
|------|------|------------|--------------|
| P-T1 | 「全流程付款中」(150) **归入「付款中」Tab**（产品 2026年7月确认） | 当前代码「付款中」`statusList` 包含 150 | **代码正确，无需改动** |
| P-T2 | 「撤销」(800) **不归入任何具体 Tab**，只在「全部」Tab 可见（需求文档 Tab 映射表中未列入任何具体 Tab） | 当前代码未放入任何具体 Tab | **代码正确，无需改动** |
| P-T3 | Tab 无交集时行为：`payManageConfig.js` 当前返回 `[]`（清空查全部），此行为**符合需求**（切到新 Tab 无交集时，不强制选中新 Tab 状态，让 `appStatus` 字段承担过滤职责） | `payManageConfig.js` 无交集返回 `[]` | **代码正确，无需改动**；两模块行为不同是合理的，`payManage` 依赖 `appStatus`，`settlement` 无 `appStatus` |
| P-T4 | `appStatus` 和 `payStatusList` **同时传给服务端即可**，前端不需要处理优先级（产品 2026年7月确认） | 两字段已同时传递 | **代码正确，无需改动** |

---

## 五、汇总待处理清单

> 所有确认项均已有结论，剩余均为前端可直接处理的代码改动。

| 优先级 | 模块 | 编号 | 事项 | 说明 |
|--------|------|------|------|------|
| P1 | 付款申请记录 | P-1 | `payManage.vue` `tabChange` 中补调 `this.$refs.searchPopup.initSearch()` | 切 Tab 后再打开弹窗时选中状态不刷新 |
| P2 | 付款申请记录 | P-2 | 移除 12 处 `payStatusList` TODO 注释 | 字段名已验证有效，`payManageConfig.js`(2处)、`payManage.vue`(4处)、`searchPopup.vue`(多处) |

---

## 六、字段确认状态汇总

| 字段 | 模块 | 确认状态 | 证据/来源 |
|------|------|---------|---------|
| `busStatusList` | 运单结算 | ✅ 已确认有效 | logisticsweb `waybillSettlement`、`driverChecking` 等多处使用 |
| `payStatusList` | 运单付款 | ✅ 已确认有效 | logisticsweb `freightNew`、`threePartyWaybillPayment` 等多处使用 |
| `payStatusList` | 付款申请记录 | ✅ 已确认有效 | 同上，`/bmsNetworkFreightPaymentDetail/page` 接口也已验证 |
| `appStatus` | 付款申请记录 | ✅ 已确认：与 `payStatusList` 同时传给服务端即可 | 产品确认，2026年7月 |

---

**文档版本**: v2.2

**最后更新**: 2026年7月

**整理人**: 王新骏
