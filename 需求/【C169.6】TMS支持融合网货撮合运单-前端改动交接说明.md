# 【C169.6】TMS支持融合网货撮合运单 - 前端改动交接说明

需求已合并入库。改动集中在 `fe-match-end/web-match-owner/apps/app-base`：`views/financial-management/` + `api/reconciliation/types/index.ts` + `api/financial-management/daily-income.ts`。

## 改动内容

| 改动 | 位置 |
|------|------|
| 客户汇总/选择运单弹窗新增「运单范围」筛选（`bizRoleList`：10=托运人运单、20=承运运单，空即全部不传参；查重口径写 sessionStorage、导出参数同步） | `customer-summary/`、`components/waybill-select-dialog.vue`、`api/reconciliation/types` |
| 业务类型文案：不开票/开票 | `customer-summary/page-config.ts` |
| 业务类型/运单类型多选改单选，`wrapSingleValueToList` 包装保持接口契约 | `customer-summary/page-config.ts` |
| 「总单数」列注释保留，拆为「承运运单」「托运人运单」两列，点击按 10/20 分流跳转运单列表；原操作列解绑（`TABLE_BUTTONS` 配置保留） | `customer-summary/` |
| 对账单加运单池 `poolIdList`：批量新增/保存带 `poolIdList`、`addPoolIdList`、`deletePoolIdList` | `components/statement-waybill-info.vue`、`customer-statement/statement.vue` |
| 日常收支订单号下拉换接口 `fetchCompanyOrderByOrderId`（`/mt-order/find-company-order-by-order-id`） | `daily-expenses`/`daily-income` 的 `receipt-detail-section.vue` |

> 「实际承运」(`carrierTypeList`) 曾加后删，代码无残留。

## 待办（接手跟进）

1. 新列字段名 `carrierWaybillCount`/`consignerWaybillCount` 为前端占位，后端确认后替换（含插槽名）
2. 新列 `:disabled="!row.xxx && hasAuth(...)"` 语义存疑，需核实
3. 托运人列表 `synchronizeWaybill/waybill-list/index.vue` 不消费 `route.query`，跳转后筛选丢失，需另行补齐
4. `TABLE_BUTTONS` 确认无用后清理

---

**文档版本**: v1.2
**最后更新**: 2026年08月
**整理人**: 王新骏
