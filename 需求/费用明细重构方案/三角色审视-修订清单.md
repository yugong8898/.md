# 费用明细重构方案 - 三角色审视修订清单

> 基于架构师、开发工程师、重构师三角色并行审视报告
> 本清单用于跟踪技术方案文档 v2.9 的修订进度

---

## 一、必须立即修正（阻塞落地）- 共 8 项

### ✅ 1. P0-10 常量遗漏：`INPUT_AMOUNT_INTEGER_MAX: 15`
**位置**：第五章 P0-10 的 `config.js` 完整内容
**问题**：常量清单里有，但 `config.js` 导出中遗漏
**修正**：在 `config.js` 的"输入框限制"部分补充此常量
**状态**：待修正

### ✅ 2. P2-1 函数命名冲突：模块级 `count` 与 mixin 方法 `count` 同名
**位置**：第七章 P2-1 的 `costCalculation.js`
**问题**：mixin 方法 `count(Amount, Rate)` 内部调用 `count(...)` 可能递归
**修正**：
- 模块级函数改名为 `calcFreightTotal`（更语义化）
- mixin 方法内调用 `calcFreightTotal(Amount, Rate, this.roundingMode)`
**状态**：待修正

### ✅ 3. P0-2 内存泄漏：`_debouncedDealZero` 需在 `beforeDestroy` 调用 `.cancel()`
**位置**：第五章 P0-2 debounce 写法修复
**问题**：组件销毁时 debounce 定时器未清理
**修正**：补充生命周期钩子
```js
beforeDestroy() {
  this._debouncedDealZero && this._debouncedDealZero.cancel()
}
```
**状态**：待修正

### ✅ 4. 依赖关系错误：P0-10 → P2-1 标注为"软依赖"实为"硬依赖"
**位置**：第九章 全局依赖关系图
**问题**：P2-1 的 `calcNetAmount` 直接 import 常量，无法在 P0-10 未完成时执行
**修正**：依赖图中改为"P0-10 完成后才能执行 P2-1"，删除"软依赖"描述
**状态**：待修正

### ✅ 5. 批次内依赖冲突：P0-1 和 P0-7 都在第 2 批但有依赖
**位置**：第五章 P0 交付顺序
**问题**：P0-7 依赖 P0-1，但两者都在"第 2 批"，并行提测会失败
**修正**：第 2 批明确标注"P0-1 必须先合入再做 P0-7"
**状态**：待修正

### ✅ 6. 数据不一致：收益表"$refs 从 7→1" vs 正文"7→3"
**位置**：第十章 收益总览 + 第七章 P2-4
**问题**：P2-4 保留 3 处 $refs（recalcZeroMode × 2 + commit × 1），但收益表说"降为 1 处"
**修正**：收益表改为"从 7 处降为 3 处（recalcZeroMode × 2 + commit × 1）"
**状态**：待修正

### ✅ 7. P1-5 遗漏：删 `dialogImgVisible` 时需同步删 `handleLookImg` 里的赋值
**位置**：第六章 P1-5 废弃字段清理
**问题**：只删除声明，未删除 `handleLookImg` 方法中的赋值语句
**修正**：P1-5 任务描述中补充"删除 handleLookImg L758 中的 `this.dialogImgVisible = ...` 赋值"
**状态**：待修正

### ✅ 8. P0-5 行为变更未说明：`break outer` 改变报错优先级
**位置**：第五章 P0-5 commit() 修复
**问题**：原 forEach 可能先报"未填写"再报"重复"，改后只报"重复"
**修正**：P0-5 "为什么安全"章节补充说明
```
⚠️ 行为变更：原 forEach 中发现重复编码后继续执行，后续行可能覆盖 n 为 '2'（未填写）；
改为 break outer 后，发现重复立即中断，优先报"重复编码"。
这是预期行为优化（重复编码比未填写更具体），符合业务逻辑。
```
**状态**：待修正

---

## 二、建议优化（不阻塞但影响质量）- 共 5 项

### 🔶 9. 批次重新分配建议
**位置**：第五章、第六章
**建议**：
- P0-10（30+ 处替换，改动量大）移到 P1 批次
- P1-5（废弃字段清理，零风险）提前到 P0 批次
- P1-6（searchOptions 行级隔离，完全独立）提前到 P0 批次
**原因**：按"改动量 + 风险"划分更合理
**是否采纳**：可选，不影响落地

### 🔶 10. 字符串匹配风险：改为按 `costCode` 匹配
**位置**：P0-1 / P2-1 calcSummaryTotal
**问题**：
- P0-1：`find(i => i.costName === '运费差价')` 用中文匹配
- P2-1：`item.costName === '油气优惠'` 用中文匹配
**建议**：
- 运费差价：按 `costCode === 'A10xxx'` 匹配（需确认运费差价的 costCode）
- 油气优惠：按 `costCode === 'A10yyy'` 匹配（需确认油气优惠的 costCode）
**是否采纳**：建议采纳，但需先确认后端返回的 costCode

### 🔶 11. P1-4 验证场景遗漏：补充 B1 企业场景
**位置**：第六章 P1-4 人工验证步骤
**问题**：只覆盖全流程运单，未覆盖 B1 企业（companyType=1）
**建议**：补充验证步骤第 5 项：
```
5. 打开一张 B1 企业运单（companyType=1）的编辑页，确认：
   - mounted 和 watcher 的 log 都出现
   - newList 里无运费差价行
   - 控制台无报错
```
**是否采纳**：建议采纳

### 🔶 12. 双轨问题无收口计划：`endOfCalculation.js` 合并时间节点
**位置**：第二章 2.3 S6 + 第七章 P2-1
**问题**：方案说"后续单独 PR 处理"，但无明确时间节点
**建议**：第十九章"落地前待确认项"补充：
```
| 3 | endOfCalculation.js 的收口计划：合并时间节点？负责人？ | 长期双轨维护成本 | P3 完成后 1 个月内完成合并 | 架构师/Tech Lead |
```
**是否采纳**：建议采纳

### 🔶 13. P1-3 描述与实现不符：`calcSubtotal` 不是纯函数
**位置**：第六章 P1-3
**问题**：方案说"写成不依赖 this 上下文的通用函数形态"，但实际有 `this.subtotal = ...` 赋值
**建议**：描述改为：
```
calcSubtotal(model) 是组件方法，有副作用（写入 this.subtotal）。
真正的纯函数是 calcNetAmount（在 P2-1 提取到 costCalculation.js）。
P1-3 的目标是让小计计算可显式调用，不依赖 el-table 渲染触发。
```
**是否采纳**：建议采纳，避免误解

---

## 三、补充说明（文档完善）- 共 3 项

### 📝 14. P2-3 附件管理迁移：`show` 对象完整结构需列入待确认项
**位置**：第十九章"落地前待确认项"
**补充**：
```
| 4 | P2-3 附件管理迁移前：show 对象的完整结构？除 show.dialog 外还有哪些字段？ | 避免把非附件字段一并迁走 | 迁移前先读代码确认 show 对象所有 key | 开发工程师 |
```

### 📝 15. P0-5 ESLint label 语法兼容性
**位置**：第五章 P0-5 + 第四章 ESLint 验证
**补充**：P0-5 "落地前必须确认"增加一条：
```
⚠️ 落地前必须确认项目 ESLint 配置是否允许 label 语法（no-labels 规则）。
如果 ESLint 禁用 label，改用以下等价写法：
let n = '', shouldBreak = false
for (const res of this.params.model) {
  if (shouldBreak) break
  // ... 校验逻辑，发现问题时 shouldBreak = true ...
}
```

### 📝 16. P0-10 debounce 来源说明
**位置**：第五章 P0-2 + 第八章 P3-1
**补充**：P0-2 开头补充说明：
```
⚠️ 前置确认：项目使用的 debounce 来源（lodash 或自实现？）
本方案假设使用 lodash debounce（支持 .cancel() 和 .flush()）。
如果是自实现的 debounce，P0-2 和 P3-1 的改法需调整。
```

---

## 修订进度跟踪

- [ ] 1. P0-10 补充 `INPUT_AMOUNT_INTEGER_MAX`
- [ ] 2. P2-1 模块级函数改名 `calcFreightTotal`
- [ ] 3. P0-2 补充 `beforeDestroy` 清理逻辑
- [ ] 4. 依赖关系图修正 P0-10 → P2-1 为硬依赖
- [ ] 5. P0 交付顺序第 2 批补充 P0-1 先合入说明
- [ ] 6. 收益表修正 $refs 降为 3 处
- [ ] 7. P1-5 补充删除 handleLookImg 赋值
- [ ] 8. P0-5 补充行为变更说明
- [ ] 9~16. 建议优化和补充说明（可选）

---

**修订目标**：完成前 8 项必须修正内容，确保方案可落地执行
**修订负责人**：王新骏
**修订完成标志**：技术方案文档更新为 v2.9，三角色审视报告归档
